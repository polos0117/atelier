# prompt.html 이미지 생성·업로드 작업 규칙

이 문서는 `polos0117/atelier`의 `prompt.html`을 이용해 이미지를 생성하고 저장소와 도감에 반영하는 작업 절차다. 이전 세션의 완료 기록이 아니라, 다음 세션에서도 재사용할 작업 규칙이다.

작업 전 [IMAGE_UPLOAD_RULES.md](IMAGE_UPLOAD_RULES.md)를 함께 읽는다. 파일명·화풍 분류·등록·중단 복구의 상세 규칙은 그 문서를 따른다. 현재 사용자의 기체·수량·화풍·연출 지시가 이 문서의 기본 작업 구성보다 우선한다.

## 1. 전체 순서

**prompt.html 설정 → 실제 출력 프롬프트 확보 → 의인화 생성 → 같은 인물의 일상컷 생성 → WebP 변환 → 기록 내보내기·원문 연결 → Git 업로드·커밋 → 도감 등록 확인**

사용자가 이 전체 작업을 요청했다면 이미지 생성만 끝내고 완료라고 말하지 않는다. 단계별 실제 상태를 기록하고 요청된 범위까지 진행한다.

## 2. prompt.html을 기준으로 생성한다

- 작업 브랜치와 기준 커밋을 확인하고 그 버전의 `prompt.html`을 사용한다. 배포 화면을 사용할 경우 작업하려는 버전과 일치하는지 확인한다.
- 브라우저 조작이 가능한 환경에서는 해당 환경의 브라우저 지침을 읽고 실제 생성기를 조작한다. 기체 선택을 적용하고, 화풍·성별·인물 설정·생성 모드를 확인한 뒤 출력 프롬프트를 확보한다.
- 기억에 의존해 비슷한 프롬프트를 새로 쓰고 `prompt.html`을 사용했다고 보고하지 않는다. 화면 접근이 안 되면 그 사실을 밝히고, 동일 코드의 로컬 실행 등 사용 가능한 방식으로 정확한 출력을 확보한다.
- 실제 생성에 제출한 프롬프트를 보존한다. AUTO가 포함됐거나 랜덤 선택이 적용된 경우 설정값만으로 같은 출력을 재구성할 수 있다고 가정하지 않는다.
- 사용자가 요청한 의상·분위기·구도·인물 조건을 임의로 완화하거나 다른 창작 방향으로 바꾸지 않는다. 적용되는 안전 규칙과 도구 제한을 따르며, 실패 시 원인을 설명하고 가능한 범위의 최소 변경을 명시한다.
- 과거 브라우저 선택값은 과거 이미지의 화풍 증거가 아니다. 이번 생성에 실제 사용한 화풍 키와 프롬프트를 함께 기록한다.

## 3. 화풍 비교 작업의 기본 구성

사용자가 이전과 같은 비교 작업을 요청하고 별도 구성을 주지 않았다면, 기체마다 다음 순서로 진행한다.

| 순서 | 화풍 | 생성물 |
|---|---|---|
| 1 | 세미리얼 시네마틱 (`cinematic_semi_real`) | 의인화 1장 |
| 2 | 같은 화풍 | 위 인물의 일상 단일컷 1장 |
| 3 | 지정한 다른 화풍. AI 선택을 맡겼으면 허용 목록에서 선택 | 같은 인물 정체성을 유지한 의인화 1장 |
| 4 | 두 번째 화풍 | 해당 인물의 일상 단일컷 1장 |

기체당 4장이다. 기체 5개면 총 20장이지만, **5개 또는 과거 기체 목록을 모든 작업의 고정값으로 삼지 않는다.** 대상과 수량은 현재 요청에 따른다.

같은 기체의 화풍 비교에서는 얼굴·헤어·성인 나이·체형 등 인물 정체성을 유지한다. 서로 다른 기체까지 같은 얼굴로 통일하지 않는다. 성별은 현재 요청과 적용된 생성기 설정을 따른다.

## 4. 포즈·시점·일상컷 규칙

| 항목 | 규칙 |
|---|---|
| 첫 화풍의 의인화 | 포즈·시점·연출은 AUTO |
| 다른 화풍의 의인화 | 동일하게 AUTO. 첫 이미지와 같은 포즈·시점을 재현할 필요 없음 |
| 화풍 간 유지할 것 | 같은 인물의 정체성과 해당 요청의 장비·기체 조건 |
| 일상컷 | 구도·포즈·시점·장소·행동은 자유. 현재 요청의 제한은 준수 |
| 일상컷 참조 | 대응하는 의인화 이미지를 실제 참조로 사용해 같은 인물 유지 |

“둘 다 AUTO”는 출력 포즈를 똑같이 맞추라는 뜻이 아니다. 프롬프트 생성기의 AUTO 설정을 유지하되 결과 포즈는 달라도 된다.

일상컷은 의인화 이미지를 단순히 배경만 바꾼 복제 컷으로 만들지 않는다. 일상 모드의 프롬프트를 사용하고, 의인화 모드의 장비나 전투 지시를 임의로 덧붙이지 않는다. 구체적인 의상·장비 유지 조건은 해당 프롬프트와 현재 요청을 따른다.

## 5. 이미지별 기록

여러 장 생성은 이미지마다 고유한 작업 ID를 부여하고, 생성 직후 대응 정보를 저장한다. 폴더의 파일 순서나 외형만으로 나중에 맞추지 않는다.

| 기록 항목 | 남길 내용 |
|---|---|
| 작업 기준 | 저장소, 브랜치, `prompt.html` 기준 커밋 |
| 생성 설정 | 카드 이름, 화풍 키, 성별, 의인화/일상/특별 모드 |
| 제출 내용 | 실제 제출한 프롬프트 전문 또는 그 파일 경로 |
| 참조 관계 | 참조 이미지 ID/파일, 일상컷의 부모 의인화 작업 ID |
| 생성 결과 | 실제 원본 위치, 도구가 반환한 생성 ID가 있으면 해당 값 |
| 최종 파일 | WebP 파일명, 픽셀 크기, 파일 해시 |
| 진행 상태 | 생성·변환·업로드·커밋·브랜치 반영·도감 등록 각각의 확인 상태 |

기록 파일과 원본/변환본은 다음 세션에서 실제로 접근할 수 있도록 보존한다. 임시 경로만 기록하거나 첨부되지 않은 파일을 저장했다고 주장하지 않는다. 저장소에 기록을 남길 때 인증정보와 만료형 서명 URL은 넣지 않는다.

이전 세션의 기록이 없으면 당시 프롬프트·기체·화풍·업로드 성공 여부를 추측해 채우지 않는다. 미확정으로 구분하고, 확인된 파일과 원격 상태부터 복구한다.

## 6. 변환·업로드·등록

- 확정 결과를 요청된 크기의 WebP로 준비한다. 크기 지정이 없으면 원본 크기를 유지한다. 비율을 보존하며 임의 크롭·늘리기·불필요한 재인코딩을 하지 않는다.
- 파일명에는 생성에 실제 사용한 화풍 키를 넣는다. 파일은 `img/`에 두고 일상컷을 대응하는 카드·화풍·성별의 `casualN`에 연결한다.
- 덮어쓰기와 슬롯 충돌을 먼저 확인하고, [IMAGE_UPLOAD_RULES.md](IMAGE_UPLOAD_RULES.md)의 등록 명령과 검증 순서를 따른다.
- 가능하면 이미지와 `data/img.json` 변경을 함께 커밋한다. 원격 브랜치에 커밋이 반영됐는지 확인한다.
- 자동 등록을 이용했다면 워크플로 실행 결과뿐 아니라 실제 등록 항목을 확인한다. 작업 브랜치에 올리는 것과 `main` 반영, 도감 등록은 서로 다른 단계다.
- 작업 범위의 기존 승인과 환경의 권한 규칙을 따른다. 이미 승인받은 같은 작업을 반복 확인하지 않으며, 권한 차단이 있으면 원인과 준비된 결과를 명확히 보고한다.

## 7. 마지막 보고

생성 수/요청 수, WebP 변환 수, 업로드 브랜치와 커밋, 도감 등록 확인 여부를 짧게 보고한다. 미확정·실패·대기 항목은 분리한다.

Blob 또는 Tree만 만들어졌거나 로컬 파일만 준비됐다면 업로드 완료가 아니다. 실제로 열어보지 못한 도감 화면이나 확인하지 않은 배포까지 완료로 보고하지 않는다.

## 8. 기록 내보내기와 도감 확인

- 생성 후 실제 UI의 ‘기록 내보내기’를 실행하고 출력 JSON을 확보한다. 버튼 클릭만으로 성공으로 간주하지 않는다.
- `toolkit-data.json`에 해당 작업의 기록을 병합한다. 기존 기체·복사 이력·과거 `style` 대장을 보존하며, 작업과 무관한 기존 설정은 덮어쓰지 않는다. 내보내기 실패는 완료 보고에서 명시한다.
- 최신 외형 설정은 이미지별 실제 제출 프롬프트와 다를 수 있다. `docs/generation/`에 제출 원문을 저장하고 `data/generation.json`에 최종 이미지 파일명별 원문 경로·매니페스트·SHA-256을 연결한다. 원문 없는 복구 이미지는 `prompt: null`로 남긴다.
- 도감 상세에서 추가 지시와 현재 선택한 이미지의 원문 링크를 확인한다. 화풍·초상·일상컷을 전환했을 때 원문도 대응해야 한다.
