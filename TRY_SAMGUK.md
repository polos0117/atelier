# 30장 시험 — 두 번째 주제가 실제로 어디서 걸리나

건담 자료를 삼국지 30장으로 갈아 끼워 `play.html` 을 돌려 본 기록이다.
목적은 게임을 하나 더 만드는 것이 아니라, **주제를 바꿀 때 코드를 몇 번 고쳐야
하는지 세는 것**이다. 그래서 규칙을 하나 정하고 시작했다 — 코드를 고칠 때마다
그것이 곧 발견이다. 미리 리팩터링하지 않고, 막힐 때만 최소로 고치고 적었다.

## 시험 자료

`data-samguk/` · 카드 30 장. `play.html?set=samguk` 으로 연다.

| 건담의 종류 | 삼국지에서 | 장수 | 능력치 |
|---|---|---|---|
| 함 | 거점 | 6 | 통솔 · 성벽 · 척후 · 주둔 |
| 기체 | 병종 | 10 | 병력 · 공격 · 수비 · 기동 |
| 파일럿 | 무장 | 10 | 통솔 · 무력 · 지력 · 정치 |
| 지휘관 | 군사 | 4 | 통솔 · 내정 · 지력 · 정치 |

기질은 그대로 쓰고(오만·냉정·폭주·헌신), 계열은 병과(보병·기병·궁병·수군·공성),
감응 등급은 신산 0~3, 감응 계통은 병법 유파(병가·도가·유가)로 옮겼다.
전장은 평지·산악·수상·성곽이다.

## 결과 — 세 번 고쳤다

| # | 고친 곳 | 줄 | 왜 |
|---|---|---|---|
| 1 | `DATA_DIR` | 5 | `?set=이름` 으로 `data-이름/` 을 읽게. 시험을 하려면 필요 |
| 2 | `take()` | 8 | 카드가 동나면 빈 자리가 편성에 들어가 화면 그릴 때 터졌다 |
| 3 | `SCHEDULE`·`PACKN`·`CAP_BONUS` | 6 | 라운드 구성이 코드에 박혀 있어 30 장으로는 판이 안 섰다 |

**셋 다 주제와 상관없는 자리였다.** 진영·능력치 이름·전장·지형·시리즈·인연은
자료만 갈아 끼우니 그대로 돌아갔다. `STAT_LABEL` 은 삼국지 이름표를 저절로
띄웠고, 전장은 평지 0.40 / 산악 0.25 / 수상 0.20 / 성곽 0.15 로 뽑혔다.

#2 는 시험이 아니었으면 못 찾았을 진짜 버그다. 건담은 기체가 765 장이라 풀이
마를 일이 없어 8 개월간 안 드러났다. 이제는 조용히 건너뛰고 콘솔에 몇 장이
모자란지 적는다.

#3 은 자료로 뺐다(`data/rule.json`). 30 장으로는 16 라운드 × 세 편 = 48 장을
못 대서 같은 카드가 두 번 뽑혔다. 시험 주제는 8 라운드로 줄여 돌린다.

## 실제로 돌아간 것

```
건담(본편)   16 라운드 · 팩 9 · 기체 765 · 끝까지 · 549 점 · 중복 0 · 오류 0
삼국지(시험)  8 라운드 · 팩 6 · 병종 10 · 끝까지 · 254 점 · 중복 0 · 오류 0
```

삼국지 판에서 조운·연노병, 제갈량·청주병 식으로 조가 짜이고, 싱크가 0.94~1.05 로
계산되고, 촉 3 · 위 2 · 군웅 2 결속이 붙고, 순위가 나온다. 규칙 코드는 한 줄도
안 고쳤다.

## 아직 걸리는 것 — 고치지 않고 적어만 둔다

### 1. 태그 인연이 통째로 죽는다 (제일 큼)

`tagsOf` 가 이름에 "자쿠"·"짐"·"건담" 이 들어가는지 보고, 계열이 `"양산"` 인지
본다. `TCOMBO` 열둘도 건담 이름이다. 삼국지에서는 태그가 붙은 병종이 **0 장**,
최대 154 점짜리 인연 체계가 통째로 안 걸린다.

```js
if(n.indexOf("자쿠")>=0)out.push("자쿠");
if(n.indexOf("건담")>=0||GUNDAM_EXTRA[n])out.push("건담");
if(c[7]==="양산")out.push("양산기");
```

고치려면 이름 규칙과 TCOMBO 를 자료로 빼야 한다. 카드에 `tags` 칸이 이미 있으니
거기서 읽게 하는 것이 자연스럽다.

### 2. 화면 문구

`기체` 69 · `파일럿` 43 · `지휘관` 25 · `보급` 14 · `감응` 11 · `혈연`·`악연` 각 10 ·
`함대` 9 · `모함` 8 · `출격` 6 · `전용기` 6 · `싱크` 4 · `기동전사` 2 · `MOBILE SUIT` 1.
합쳐 190 자리. 삼국지 판에서도 "모함 건업", "기동전사 드래프트" 로 뜬다.

동작에는 지장이 없다. 종류 이름 넷은 `KIND` 가 정하지만 화면 문구는 아직
사람이 읽는 말이라 따로 있다. 낱말 표 하나로 뺄 수 있는 일이다.

### 3. 업적 여덟

`FEATS` 의 "첫 출격"·"에이스를 꺾다"·"관제 두절" 은 건담 말이다. 규칙 자체는
주제와 무관하다(수위·점수차·인연 수).

### 4. 자료 모양이 적혀 있지 않다

`combo.json` 의 레코드가 `{n, m, t, k}` 인 것을 몰라 처음에 배열로 만들었다가
`pledges` 에서 터졌다. 각 파일 머리말의 `note` 에 모양을 적어 두면 다음 사람이
같은 데서 안 넘어진다. `combo.json` 은 적혀 있었고 내가 안 읽었다 — 다른 파일도
같은 수준인지 볼 것.

## 그래서 얼마나 드나

**코드 19 줄.** 자료 30 장을 만드는 데 든 시간이 코드 고치는 시간보다 훨씬 길었다.
"저장소를 통째로 복사해 고치는 방식이 된다" 는 말은 사실이 아니었고, 남은 결합
셋 중 실제로 게임을 망가뜨리는 것은 태그 인연 하나뿐이다.

두 번째 주제를 진짜로 만들 때 순서는 이렇게 본다.

```
1. 태그 인연을 자료로              — 안 하면 점수 체계 한 축이 죽는다
2. 화면 낱말 표                    — 안 해도 굴러가지만 남 보여주긴 어렵다
3. 업적                            — 마지막. 규칙은 그대로고 이름만이다
```

`mp`·`pp`·`sp`·`cp` 와 카드 배열 넷은 이번에도 손댈 일이 없었다. 능력치가 넷이고
종류가 넷인 주제라면 그대로 쓰인다. 그 전제를 깨는 주제를 만나기 전에 미리
빼는 것은 여전히 이르다.
