# 검토서 대조 결과

검토서 두 편(`play.html` 밸런스, `prompt.html` 개선안)의 항목을 실제 코드와 하나씩 맞춰 본 결과다.
두 문서 모두 "적용하기 전에 사실관계를 다시 확인하라" 고 적어 두었으므로 그대로 했다.

판정은 다섯 가지다.

- **확인됨** — 지적이 맞다. 고칠 것
- **이미 해결됨** — 그 사이 고쳐졌다
- **의도된 동작** — 그렇게 만든 것이다
- **수정 필요(수치 정정)** — 지적은 맞으나 근거 숫자가 틀렸다
- **보류** — 맞지만 지금 손대면 손해

---

## play.html

| 항목 | 판정 | 근거 |
|---|---|---|
| P1 `?set=` 별 전적 분리 | **확인됨** | `REC_KEY="gundam_draft_rec_v1"` 고정. `DATA_DIR` 만 `?set=` 을 따른다 |
| P2 `validateData()` 부재 | **확인됨** | 그런 함수가 없다. fetch 실패만 `boot()` 이 잡는다 |
| P3 seed / replay | **확인됨** | `Math.random()` 9곳이 직접 쓰인다. 재현 수단 없음 |
| P4 밸런스 상수 외부화 | **보류** | 검토서도 "실제 조정 시작할 때" 로 미뤘다. 지금은 이르다 |
| P5 화면 용어 데이터화 | **보류** | 동작을 깨지 않는다. 두 번째 주제를 제품으로 붙일 때 |
| S1 `assign()` 탐욕 | **확인됨** | 짝 점수 내림차순으로 훑으며 양쪽이 비었으면 채택. 총합 최적이 아니다 |
| S2 숙련 AI가 인연 무시 | **확인됨** | `var blind=AI_LV<2` — 신참·숙련이 함께 눈을 감는다 |
| A1 지형이 상한 안에 갇힘 | **확인됨** | `sync()` 가 `1+t+n+s+o+b+g` 를 통째로 `[.70,1.45]` 로 자른다. 지형 `g` 가 상한 안에 있다 |
| A2 정원 3→4 절벽 | **수정 필요(수치 정정)** | 아래 별항 |
| A3 AI 가 정원의 미래값을 못 본다 | **확인됨** | `sp(s)=s[2]*.5+s[3]*.3+s[4]*.2` 로 정원 `s[5]` 가 빠져 있고, 1라운드엔 기체·파일럿이 없어 `capOf` 도 점수에 안 잡힌다 |
| B1 태그 인연 천장 | **수정 필요(수치 정정)** | 아래 별항 |
| B2 견제 없음 | **확인됨** | `aiPick` 은 자기 팀 증가분만 본다 |
| B3 franchise 정규화 | **확인됨** | `normalize()` 가 `T / (그 franchise 최고값)` 을 그 franchise 전원에 곱한다. 최고 카드를 깎으면 같은 franchise 전체가 올라간다 |
| C1 파일럿 네 번째 스탯 | **확인됨** | `pp()` 는 `p[2],p[3],p[4]` 만 쓴다. 정치 `p[5]` 는 게임 어디서도 안 읽힌다 (12~96, 평균 45.4 로 폭이 넓어 화면에선 의미 있어 보인다) |
| §6 pool/discard 재합류 | **확인됨 (버그)** | 검토서는 "확인 후보" 로 남겼으나 실제 버그다. 아래 별항 |

### A2 정정 — 절벽은 맞으나, 더 심하다

검토서는 "정원 3 → cap 5, 정원 4 → cap 6" 까지만 봤다. 실제 모함 79척의 정원 분포는:

```
정원 2 : 1척     3 : 13척     4 : 41척     5 : 19척     6 : 5척
CAP_BONUS=2 → 실제 정원 4 / 5 / 6 / 7 / 8
```

최대 편성이 6조이므로:

```
정원 2 (1척)  → 2조가 60%
정원 3 (13척) → 1조가 60%
정원 4 (41척) → 전부 100%
정원 5 (19척) → 정원 4와 완전히 같다
정원 6 (5척)  → 정원 4와 완전히 같다
```

즉 **79척 중 65척(82%)이 이 축에서 서로 구별되지 않는다.** 곡선이 끊긴 정도가 아니라
정원이라는 스탯 자체가 대부분의 모함에서 죽어 있다. 실제로 갈리는 것은 14척뿐이다.

### B1 정정 — 220.5 는 나올 수 없는 숫자다

검토서는 `건담 6(135) + 백색 5(100) + 주역기 3(80)` 을 더해 220.5 를 냈다.
그러나 색 태그는 서로 배타적이라 (백색·적색·흑색·청색·녹색·금색) 한 편성이
여러 색 인연을 동시에 크게 받기 어렵다. 실제 천장은 로스터에서 직접 재야 한다.

검토서도 "지금 너프하지 말고 먼저 재라" 고 했으므로 **측정만 하고 수치는 건드리지 않는다.**

### §6 정정 — 후보가 아니라 확정 버그

경로를 따라가면 이렇다.

```
drawPack()  pools 에서 뽑되 pools 에서 빼지 않는다
take()      집은 카드만 pools 에서 splice
tossRest()  안 팔린 카드를 discard 로 밀어 넣는다  ← pools 에는 그대로 남아 있다
drawPack()  pools 가 얇아지면 pools = pools.concat(discard)
```

안 팔린 카드는 `pools` 에 남은 채로 `discard` 에도 들어간다. 합류하는 순간 **같은 카드가
`pools` 에 둘이 된다.** 한 팩에 같은 카드가 두 장 뜨고, `take()` 는 하나만 지운다.

본편에서는 안 터진다 — 합류 조건이 `pools.length < PACKN+2` 인데 765기·210명·79척·33명
모두 그 밑으로 내려가지 않는다. 30장짜리 시험 주제에서만 드러난다.

---

## prompt.html

| 항목 | 판정 | 근거 |
|---|---|---|
| 2.1 Single refresh 오배선 | **확인됨 (더 나쁨)** | 아래 별항 |
| 2.2 carry 가 성별을 안 따른다 | **확인됨** | `const g=r.female?'female':(r.male?'male':null)` — 화면이 남성이어도 여성 기록이 이긴다. `ethnicityOf()` 도 같은 줄 |
| 2.3 자동 선택 경로에 성별 필터 없음 | **확인됨** | `CAT_ROWS()`/`EX_ROWS()` 는 화면에만 걸린다. `RANDOM_MODES.*.pool` 은 날 문자열 배열이고 `stable` 여섯 중 넷이 여성 전용, `sourceRecommendations()` 의 `valid()`·`catOverride` 는 `EXAMPLE_MAP` 을 그대로 읽는다 |
| 2.4 `occupation_sensual` 여성 하드코딩 | **일부 이미 해결됨 + 확인됨** | 검토서가 든 `clearly adult woman` 셋·`her`·`bust` 는 v9.11 에서 이미 고쳤다. 남은 것은 `occupation_sensual` 안내문의 `the same clearly adult woman` **한 곳** |
| 2.5 복사 실패해도 사용 기록 | **확인됨** | `copyPrompt()` 를 `await` 하지 않고 바로 `recordUse()`/`sgRecordUse()` 를 부른다 |
| 2.6 새 기체에서 앞 기체 값 상속 | **확인됨** | 주석은 "기본값으로 되돌린다" 인데 `if(!rec){updateSaveBar();return false;}` 로 아무것도 안 되돌리고 나간다 |
| 2.7 사용 이력 성별 분리 | **보류** | 검토서도 "남성 사용이 늘어난 뒤" 로 미뤘다 |
| 3.1 Identity Lock 화풍 종속 | **확인됨** | 단일·콜라주 두 곳에 `translated into a more refined anime-inspired visual language` |
| 3.2 Anthro AVOID 배경 강제 | **확인됨 (자기모순)** | 같은 `templateC` 안에서 LIGHTING/BACKGROUND 는 `paper-like negative space` 를 허용하는데 AVOID 는 `bright background, white background` 를 금지한다 |
| 3.3 NON-EXPLICIT LIMITS | **확인됨** | 두 곳 모두 허용 소재를 나열한다. `everyday_basic` 에도 `lingerie-inspired fashion` 이 실려 나간다 |
| 4 NAME_RULES first-match | **확인됨** | 출력 오류는 아니다. 검토서도 P0/P1 뒤로 미뤘다 |
| 6 ART_STYLES 죽은 사본 | **확인됨 (이미 보고함)** | `ART_STYLES[i][3]` 에 11개 화풍 본문이 통째로 복사돼 있고 아무 데서도 안 읽는다 |

### 2.1 정정 — 배선만 어긋난 게 아니다

```js
if(textId==='textCollage')refreshCollagePrompt();else refreshAnthroPrompt();
```

`textSingle` 이 `else` 로 떨어져 **엉뚱한 프롬프트를 새로 짜고**, 정작 복사하는 것은
`textSingle` 의 묵은 값이다. 게다가 복사가 막혔을 때의 갈래가

```js
const viewId=textId==='textCollage'?'viewCollage':'viewAnthro';
```

이라, 단일 컷에서 복사가 막히면 **의인화 텍스트 상자가 열리고** 정작 `textSingle` 은
숨은 채로 `select()` 를 당한다. 숨은 요소의 선택은 조용히 실패한다.

`toggleView()` 도 같은 갈래를 쓴다.

---

## 작업 순서

검토서가 정한 순서를 그대로 따른다. 밸런스 숫자는 계산 정확성과 AI 가 안정된 뒤에 만진다.

1. **1단계 — 계산 정확성·재현성**: S1 최적 편성 · P3 seed · P2 검증 · P1 set별 기록 · §6 중복
2. **2단계 — prompt.html 기능 정확성**: 2.1 · 2.2 · 2.3 · 2.4 · 2.5 · 2.6
3. **3단계 — AI 규칙**: S2 · A3 · B2
4. **4단계 — 점수 구조**: A1 · A2 · B1 측정
5. **5단계 — 프롬프트 충돌**: 3.1 · 3.2 · 3.3 · audit 강화
6. **6단계 — 유지보수**: 6(죽은 사본) · 4(NAME_RULES) · C1 판단

각 단계마다 건담 본편이 같은 seed 에서 같은 결과를 내는지 대조한다.
