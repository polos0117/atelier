# 검토서 대조 결과

검토서 두 편(`play.html` 밸런스, `prompt.html` 개선안)의 항목을 실제 코드와 하나씩 맞춰 본 결과다.
두 문서 모두 "적용하기 전에 사실관계를 다시 확인하라" 고 적어 두었으므로 그대로 했다.

판정은 다섯 가지다.

- **확인됨** — 지적이 맞다. 고칠 것
- **이미 해결됨** — 그 사이 고쳐졌다
- **의도된 동작** — 그렇게 만든 것이다
- **수정 필요(수치 정정)** — 지적은 맞으나 근거 숫자가 틀렸다
- **보류** — 맞지만 지금 손대면 손해

---

## play.html

| 항목 | 판정 | 근거 |
|---|---|---|
| P1 `?set=` 별 전적 분리 | **확인됨** | `REC_KEY="gundam_draft_rec_v1"` 고정. `DATA_DIR` 만 `?set=` 을 따른다 |
| P2 `validateData()` 부재 | **확인됨** | 그런 함수가 없다. fetch 실패만 `boot()` 이 잡는다 |
| P3 seed / replay | **확인됨** | `Math.random()` 9곳이 직접 쓰인다. 재현 수단 없음 |
| P4 밸런스 상수 외부화 | **보류** | 검토서도 "실제 조정 시작할 때" 로 미뤘다. 지금은 이르다 |
| P5 화면 용어 데이터화 | **보류** | 동작을 깨지 않는다. 두 번째 주제를 제품으로 붙일 때 |
| S1 `assign()` 탐욕 | **확인됨** | 짝 점수 내림차순으로 훑으며 양쪽이 비었으면 채택. 총합 최적이 아니다 |
| S2 숙련 AI가 인연 무시 | **확인됨** | `var blind=AI_LV<2` — 신참·숙련이 함께 눈을 감는다 |
| A1 지형이 상한 안에 갇힘 | **확인됨** | `sync()` 가 `1+t+n+s+o+b+g` 를 통째로 `[.70,1.45]` 로 자른다. 지형 `g` 가 상한 안에 있다 |
| A2 정원 3→4 절벽 | **수정 필요(수치 정정)** | 아래 별항 |
| A3 AI 가 정원의 미래값을 못 본다 | **확인됨** | `sp(s)=s[2]*.5+s[3]*.3+s[4]*.2` 로 정원 `s[5]` 가 빠져 있고, 1라운드엔 기체·파일럿이 없어 `capOf` 도 점수에 안 잡힌다 |
| B1 태그 인연 천장 | **수정 필요(수치 정정)** | 아래 별항 |
| B2 견제 없음 | **확인됨** | `aiPick` 은 자기 팀 증가분만 본다 |
| B3 franchise 정규화 | **확인됨** | `normalize()` 가 `T / (그 franchise 최고값)` 을 그 franchise 전원에 곱한다. 최고 카드를 깎으면 같은 franchise 전체가 올라간다 |
| C1 파일럿 네 번째 스탯 | **확인됨** | `pp()` 는 `p[2],p[3],p[4]` 만 쓴다. 정치 `p[5]` 는 게임 어디서도 안 읽힌다 (12~96, 평균 45.4 로 폭이 넓어 화면에선 의미 있어 보인다) |
| §6 pool/discard 재합류 | **확인됨 (버그)** | 검토서는 "확인 후보" 로 남겼으나 실제 버그다. 아래 별항 |

### A2 정정 — 절벽은 맞으나, 더 심하다

검토서는 "정원 3 → cap 5, 정원 4 → cap 6" 까지만 봤다. 실제 모함 79척의 정원 분포는:

```
정원 2 : 1척     3 : 13척     4 : 41척     5 : 19척     6 : 5척
CAP_BONUS=2 → 실제 정원 4 / 5 / 6 / 7 / 8
```

최대 편성이 6조이므로:

```
정원 2 (1척)  → 2조가 60%
정원 3 (13척) → 1조가 60%
정원 4 (41척) → 전부 100%
정원 5 (19척) → 정원 4와 완전히 같다
정원 6 (5척)  → 정원 4와 완전히 같다
```

즉 **79척 중 65척(82%)이 이 축에서 서로 구별되지 않는다.** 곡선이 끊긴 정도가 아니라
정원이라는 스탯 자체가 대부분의 모함에서 죽어 있다. 실제로 갈리는 것은 14척뿐이다.

### B1 정정 — 220.5 는 나올 수 없는 숫자다

검토서는 `건담 6(135) + 백색 5(100) + 주역기 3(80)` 을 더해 220.5 를 냈다.
그러나 색 태그는 서로 배타적이라 (백색·적색·흑색·청색·녹색·금색) 한 편성이
여러 색 인연을 동시에 크게 받기 어렵다. 실제 천장은 로스터에서 직접 재야 한다.

검토서도 "지금 너프하지 말고 먼저 재라" 고 했으므로 **측정만 하고 수치는 건드리지 않는다.**

### §6 정정 — 후보가 아니라 확정 버그

경로를 따라가면 이렇다.

```
drawPack()  pools 에서 뽑되 pools 에서 빼지 않는다
take()      집은 카드만 pools 에서 splice
tossRest()  안 팔린 카드를 discard 로 밀어 넣는다  ← pools 에는 그대로 남아 있다
drawPack()  pools 가 얇아지면 pools = pools.concat(discard)
```

안 팔린 카드는 `pools` 에 남은 채로 `discard` 에도 들어간다. 합류하는 순간 **같은 카드가
`pools` 에 둘이 된다.** 한 팩에 같은 카드가 두 장 뜨고, `take()` 는 하나만 지운다.

본편에서는 안 터진다 — 합류 조건이 `pools.length < PACKN+2` 인데 765기·210명·79척·33명
모두 그 밑으로 내려가지 않는다. 30장짜리 시험 주제에서만 드러난다.

---

## prompt.html

| 항목 | 판정 | 근거 |
|---|---|---|
| 2.1 Single refresh 오배선 | **확인됨 (더 나쁨)** | 아래 별항 |
| 2.2 carry 가 성별을 안 따른다 | **확인됨** | `const g=r.female?'female':(r.male?'male':null)` — 화면이 남성이어도 여성 기록이 이긴다. `ethnicityOf()` 도 같은 줄 |
| 2.3 자동 선택 경로에 성별 필터 없음 | **확인됨** | `CAT_ROWS()`/`EX_ROWS()` 는 화면에만 걸린다. `RANDOM_MODES.*.pool` 은 날 문자열 배열이고 `stable` 여섯 중 넷이 여성 전용, `sourceRecommendations()` 의 `valid()`·`catOverride` 는 `EXAMPLE_MAP` 을 그대로 읽는다 |
| 2.4 `occupation_sensual` 여성 하드코딩 | **일부 이미 해결됨 + 확인됨** | 검토서가 든 `clearly adult woman` 셋·`her`·`bust` 는 v9.11 에서 이미 고쳤다. 남은 것은 `occupation_sensual` 안내문의 `the same clearly adult woman` **한 곳** |
| 2.5 복사 실패해도 사용 기록 | **확인됨** | `copyPrompt()` 를 `await` 하지 않고 바로 `recordUse()`/`sgRecordUse()` 를 부른다 |
| 2.6 새 기체에서 앞 기체 값 상속 | **확인됨** | 주석은 "기본값으로 되돌린다" 인데 `if(!rec){updateSaveBar();return false;}` 로 아무것도 안 되돌리고 나간다 |
| 2.7 사용 이력 성별 분리 | **보류** | 검토서도 "남성 사용이 늘어난 뒤" 로 미뤘다 |
| 3.1 Identity Lock 화풍 종속 | **확인됨** | 단일·콜라주 두 곳에 `translated into a more refined anime-inspired visual language` |
| 3.2 Anthro AVOID 배경 강제 | **확인됨 (자기모순)** | 같은 `templateC` 안에서 LIGHTING/BACKGROUND 는 `paper-like negative space` 를 허용하는데 AVOID 는 `bright background, white background` 를 금지한다 |
| 3.3 NON-EXPLICIT LIMITS | **확인됨** | 두 곳 모두 허용 소재를 나열한다. `everyday_basic` 에도 `lingerie-inspired fashion` 이 실려 나간다 |
| 4 NAME_RULES first-match | **확인됨** | 출력 오류는 아니다. 검토서도 P0/P1 뒤로 미뤘다 |
| 6 ART_STYLES 죽은 사본 | **확인됨 (이미 보고함)** | `ART_STYLES[i][3]` 에 11개 화풍 본문이 통째로 복사돼 있고 아무 데서도 안 읽는다 |

### 2.1 정정 — 배선만 어긋난 게 아니다

```js
if(textId==='textCollage')refreshCollagePrompt();else refreshAnthroPrompt();
```

`textSingle` 이 `else` 로 떨어져 **엉뚱한 프롬프트를 새로 짜고**, 정작 복사하는 것은
`textSingle` 의 묵은 값이다. 게다가 복사가 막혔을 때의 갈래가

```js
const viewId=textId==='textCollage'?'viewCollage':'viewAnthro';
```

이라, 단일 컷에서 복사가 막히면 **의인화 텍스트 상자가 열리고** 정작 `textSingle` 은
숨은 채로 `select()` 를 당한다. 숨은 요소의 선택은 조용히 실패한다.

`toggleView()` 도 같은 갈래를 쓴다.

---

## 작업 순서

검토서가 정한 순서를 그대로 따른다. 밸런스 숫자는 계산 정확성과 AI 가 안정된 뒤에 만진다.

1. **1단계 — 계산 정확성·재현성**: S1 최적 편성 · P3 seed · P2 검증 · P1 set별 기록 · §6 중복
2. **2단계 — prompt.html 기능 정확성**: 2.1 · 2.2 · 2.3 · 2.4 · 2.5 · 2.6
3. **3단계 — AI 규칙**: S2 · A3 · B2
4. **4단계 — 점수 구조**: A1 · A2 · B1 측정
5. **5단계 — 프롬프트 충돌**: 3.1 · 3.2 · 3.3 · audit 강화
6. **6단계 — 유지보수**: 6(죽은 사본) · 4(NAME_RULES) · C1 판단

각 단계마다 건담 본편이 같은 seed 에서 같은 결과를 내는지 대조한다.

---

# 진행

## 1단계 — 계산 정확성·재현성 (끝)

| 항목 | 커밋 | 재고 확인 |
|---|---|---|
| §6 버림패 중복 | `3adfe48` | 시험 주제에서 한 팩 안 중복 2094→0. 본편도 물린 카드 재등장 3.1%→0 |
| P3 씨앗·재현 | `2ad9ee1` | 같은 씨앗이 전장·좌석 순서·마흔여덟 수·최종 점수까지 그대로 |
| 지난 판 타이머 (검토서 밖) | `31b9a94` | 적 차례에 새 판을 열면 첫 라운드를 타이머가 두고 지나가던 것 |
| S1 최적 편성 | `7db45d5` | 편성 180개가 모두 완전탐색과 같은 값. 한 수 1.4ms |
| P2 자료 검사 | `d5b68e4` | 열두 곳을 망가뜨려 확인. 본편·시험 주제는 오탐 0 |
| P1 주제별 기록 | 이 커밋 | 다섯 갈래로 확인 |

### 재보니 검토서와 달랐던 값

- **S1 손해**: 검토서 "약 4% 이상" → 실측 **평균 0.53%** (2.81점/531점).
  다만 편성 41%에서 손해가 나고 가장 큰 것이 28.3점이라, 판이 수십 점 차로
  갈리는 것을 감안하면 고칠 값어치는 있었다. 검토서의 4% 는 골라 든 사례였다.
- **일부러 대기시키는 수**를 허용해도 0.24% 밖에 더 벌지 못했다. 그래서
  "짝은 될 수 있는 대로 많이" 라는 지금 규칙을 그대로 두었다.
- **§6** 은 검토서가 "확인 후보" 로 남겼으나 확정 버그였고, 본편에도 3.1% 로
  드러나 있었다.

### 아직 재지 못한 것

같은 씨앗으로 편성 변경의 이득을 따로 떼어 재려 했으나 안 된다. 적 함대의
판단이 편성 점수 위에 서기 때문에 첫 수부터 판이 갈라진다. 예순 판이 모두
다른 판이 되었다. 떼어 낸 값은 위의 2.81점이 전부다.

## 다음 — 2단계 prompt.html 기능 정확성

2.1 단일 컷 배선 · 2.2 성별 기준 · 2.3 자동 선택 성별 필터 ·
2.4 여성 하드코딩 한 곳 · 2.5 복사 성공 뒤에만 기록 · 2.6 새 기체 초기화

## 3단계 — AI 규칙 (진행 중)

| 항목 | 커밋 | 재고 확인 |
|---|---|---|
| S2 적장의 격에 따라 인연을 보는 눈 | `31419e1` | 사다리 88 · 53 · 32.5 로 벌어짐 |
| A3 정원의 앞일 | 이 커밋 | 아래 별항 — 데이터 쪽이 먼저였다 |
| B2 에이스 견제 | 아직 | |

### 재는 도구가 흔들리고 있었다

같은 파일을 같은 씨앗으로 세 번 돌렸더니 숙련 승률이 61.7 · 50.0 · 60.0 으로
나왔다. `reset()` 이 화면을 그리며 첫 팩을 미리 뽑는데, 그것이 씨앗을 심기 전에
뽑힌 것이라 판마다 달랐다. 씨앗을 심은 뒤 그 팩을 버리게 하니 세 번 모두 같아졌다.

**이 잘못을 찾기 전에 잰 숫자는 모두 버렸다.** 아래는 고친 뒤에 다시 잰 것이다.

### 노이즈 바닥

이백 판에서 승률의 표준편차는 판 수로 약 6.6, 곧 3.3%p 다. **6.5%p 안쪽의 차이는
읽지 않는다.** 이 기준을 세운 뒤에야 "신참과 에이스는 한 판도 안 바뀌었다" 같은
말을 할 수 있게 되었다.

### A3 — 코드가 아니라 데이터가 먼저였다

검토서는 "AI 가 정원의 미래 가치를 못 본다" 를 코드 문제로 보았다. 재어 보니
그 앞에 데이터 문제가 있었다.

```
모함 79척 정원 분포 : 2:1  3:13  4:41  5:19  6:5
CAP_BONUS = 기체 자리(6) − 4 = 2
실제 정원 = 정원+2,  최대 편성 6조
→ 정원 4 이상이면 모두 6조를 채우고도 남는다. 65척(82%)이 이 축에서 같다.
```

그래서 코드에 박혀 있던 `4` 를 `rule.json` 의 `cap_base` 로 뺐다. 기본값 4 는
지금 판과 한 판도 다르지 않다.

값을 바꿔 가며 이백 판씩 재었다.

| cap_base | 정원초과 판 | 아군 평균점 | 고른 배의 원래 정원 |
|---|---|---|---|
| 4 (지금) | 4~11 | 899~906 | 4.64 |
| 5 | 103~152 | 883~891 | 4.64 |
| 6 | 155~164 | 861~867 | 4.64 |

**고른 배의 정원이 셋 다 4.64 로 꿈쩍하지 않는다.** 정원이 아무리 중요해져도
아무도 정원 높은 배를 더 고르지 않는다. 앞일을 못 보기 때문이다. 데이터만
바꾸면 전원에게 걷는 세금이 될 뿐이다.

그래서 앞일을 보는 눈(`capForesight`)을 함께 넣고, 사람에게도 같은 것을 보이도록
모함 카드에 말로 적었다. 점수는 건드리지 않는다 — 그 손해는 뒷날 실제로 일어날
때 이미 점수에 잡히므로, 미리 얹으면 두 번 세는 셈이다.

| | 정원초과 판 | 승률 (신참/숙련/에이스) |
|---|---|---|
| cap_base 4 · 안내 무시 | 4~11 | 88.0 / 53.5 / 34.0 |
| cap_base 4 · 안내 읽음 | 0 | 88.5 / 53.0 / 33.5 |
| cap_base 5 · 안내 무시 | 103~152 | 87.0 / 53.0 / 28.0 |
| cap_base 5 · 안내 읽음 | 14~50 | 89.5 / 50.5 / 29.0 |

cap_base 4 에서는 안내를 읽어도 승률이 노이즈 안에서 움직인다 — 정원은 여전히
죽은 스탯이다. cap_base 5 에서야 읽는 것이 이득이 된다: 정원초과가 152 판에서
50 판으로 줄고, 신참을 상대로 87 에서 89.5 로 오른다.

**cap_base 는 5 로 정했다.** 편성 규모를 고르게 만들면서 함께 결정했다 —
자리를 늘려도 웃돈이 따라 커져 정원은 여전히 죽어 있었기 때문이다.

| cap_base 5 · 6조 | 정원초과 판 | 아군 평균점 | 승률 (신참/숙련/에이스) |
|---|---|---|---|
| 안내 무시 | 103~152 | 882~891 | 87.0 / 54.0 / 27.5 |
| 안내 읽음 | 14~50 | 884~905 | 90.0 / 51.5 / 29.0 |

읽는 쪽이 정원초과를 152 판에서 50 판으로 줄이고 신참 상대 승률을 87 에서 90 으로
올린다. 승률 차이는 대부분 노이즈 안이지만 정원초과 판 수는 세 배 차이라 뚜렷하다.

규모마다 아군이 집는 배의 평균 정원이 6.00 · 8.00 · 10.00 으로 필요한 만큼에
정확히 붙는다. cap_base 4 에서는 6.65 · 8.65 · 10.65 로 아무 생각 없이 넘쳤다.

## 4단계 — 점수 구조

| 항목 | 커밋 | 재고 확인 |
|---|---|---|
| A1 지형이 상한에 갇힘 | `6a1170b` | 사라지던 감점 57 → 1. 밸런스는 노이즈 안 |
| B1 태그 인연 천장 | 측정만 | 아래 |
| A2 모함 정원 절벽 | `138feb1` 에서 자료로 뺌 | `cap_base` 는 4 로 둔다 |

### B1 — 천장을 실제로 재었다

검토서는 `건담 6(135) + 백색 5(100) + 주역기 3(80)` 을 더해 220.5 를 냈다. 나는
색 태그가 서로 배타적이라 그 조합이 불가능하다고 적었다. **둘 다 틀렸다.**
로스터에서 여섯 장을 골라 태그 인연을 최대로 하면 이렇게 된다.

```
사자비 · 나이팅게일 · 저스티스 · 스트라이크 루즈 · 건담 데스사이즈 · 건담 데스사이즈 헬
  붉은 기체들 4장 92 / 검은 기체들 2장 30 / 건담이라는 이름 6장 135
  / 주역의 자리 3장 80 / 숙적들 3장 80
  = 417  ×0.7 = 291.9점
```

색이 배타적인 것은 맞지만, **두 색을 넷·둘로 쪼개어 둘 다 받는 길**이 있었다.
검토서의 220.5 는 과소평가였다.

실전에서는 이렇다 (에이스 이백 판, 편성 육백 개).

| | 값 | 한 조(85.9점) 대비 |
|---|---|---|
| 태그 인연 평균 | **144.0점** | 1.7조 |
| 태그 인연 최대 | 266.0점 | 3.1조 |
| 이름 인연 평균 | 73.3점 | 0.9조 |
| 결속 평균 | 34.0점 | 0.4조 |

**태그 인연이 이름 인연의 두 배**이고, 편성 하나가 태그만으로 조 두 개에 가까운
값을 얻는다. 검토서의 걱정은 방향이 맞다.

### 곁가지 — `gundam` 표가 무엇을 뜻하는가

`data/mech.json` 의 서른다섯 기에 `"gundam": true` 가 붙어 있다. 모두 이름에
건담이 없는 기체다. 스트라이크 · 프리덤 · 저스티스 · 톨기스 · 더블 엑스처럼
원작에서 건담인 것도 있으나, **지옹 · 큐베레이 · 디 오 · 사자비 · 나이팅게일 ·
네오 지옹**처럼 건담이 아닌 것도 함께 들어 있다. 그래서 사자비가 "건담이라는
이름" 인연에 든다.

표를 떼고 이름만으로 태그를 붙여 다시 재었다.

| | 천장 | 실전 평균 | 실전 최대 |
|---|---|---|---|
| 지금 (표 있음) | 291.9 | 144.0 | 266.0 |
| 표 없이 | **291.9** | 120.1 | 231.0 |

**천장은 한 점도 안 바뀐다.** 표가 없어도 이름만으로 여섯 장을 채울 수 있기
때문이다. 표가 바꾸는 것은 천장이 아니라 잦기다 — 실전 평균이 20% 낮아진다.

즉 `gundam` 표는 밸런스 문제가 아니라 **이름의 문제**다.

**뒤에 정리했다.** 지온·네오지온 계열 여덟 기 — 지옹 · 사자비 · 나이팅게일 ·
네오 지옹 · 시난주 · 크샤트리야 · 큐베레이 · 디 오 — 에서 표를 뗐다. 건담 타입이
아닌 기체들이다. 건담 태그를 받는 기체가 238 기에서 230 기가 되었고, 실전 태그
인연 평균이 144.0 에서 135.9 로 5.6% 내렸다. 천장 291.9 와 최대 266.0 은 그대로다.

**그 뒤 공식 자료로 다시 찍었다.** 손으로 판단할 일이 아니었다 — 저장소가 이미
G 제네레이션 이터널 API 를 받아 `.cache/soshage/unit.json` 에 두고 있고, 유닛마다
`tags` 가 붙어 있으며 그 가운데 id 1003 이 "건담" 이다. 482 기에 붙어 있다.

`id-map.json` 으로 우리 카드와 짝지으면 765 기 가운데 464 기가 대조된다. 그
464 기를 견주니 이렇게 나왔다.

| | 기수 | |
|---|---|---|
| 여태 놓쳤다 | 9 | 릭 디아스 · **스트라이크 프리덤** · **더블오 라이저** · 델타 플러스 · 리젤 · 아류제우스 · GN 아머 TYPE-D · TYPE-E · M1 어스트레이(SHRIKE) |
| 이름에 건담이 들었으나 공식은 아니다 | 1 | 제스타 건담 |
| 손으로 붙였으나 공식은 아니다 | 6 | 제간 · 미카엘리스 · 톨기스 · 톨기스 II · 톨기스 III · 아델 |

내가 앞서 "애매하다" 고 적은 아홉 기 가운데 **백식과 리가지는 공식이 건담이라
한다.** 내 짐작이 틀렸다. 반대로 스트라이크 프리덤과 더블오 라이저는 이름에
건담이 없다는 이유로 태그를 못 받고 있었다 — 손으로 붙이는 방식의 한계다.

`build-gge-tags.py` 가 이 일을 한다. 짝이 없는 301 기는 건드리지 않는다.
짝이 없어 손으로 붙인 표가 남은 것은 셋이다 — **밴시 노른 · 더블 엑스 · 레길스**.
앞의 둘은 건담이 맞고 레길스는 아니지만, 근거가 될 공식 자료가 없어 두었다.

### 곁가지 — 이름 오류 하나를 찾아 고쳤다

`제스타 건담` 이 공식으로 "건담이 아니다" 라고 나왔다. 카드를 열어 보니 소속이
재야, 시리즈가 G, 시스템이 모빌트레이스였다. **제스타가 아니라 G건담의 기체다.**

원인은 `models` 였다. `RGM-96X` 는 제스타(짐 계열 양산기)의 형식번호인데 이 카드에
잘못 붙어 있었고, 그 하나 때문에 형식번호로 엉뚱하게 짝지어졌다. 그래서 앞선
커밋에서 내가 찍은 `gundam: false` 도 틀린 것이었다.

이름도 틀렸다. ジェスターガンダム — 네오 포르투갈의 모빌 파이터이므로 **제스터
건담**이다. G 제네레이션에 참전하지 않아 공식 자료에 아예 없다.

셋을 함께 고쳤다.

- 카드에서 잘못된 `models` 와 `gundam` 을 뗐고 이름을 `제스터 건담` 으로 바로잡았다
- `id-map.overrides.json` 에 `null` 을 적을 수 있게 하여 "API 에 짝이 없다" 를
  사람이 못박을 수 있게 했다. 형식번호나 이름 앞머리가 우연히 겹쳐 엉뚱한 유닛에
  붙는 것을 막는 자리다
- 이 카드는 그림도 인연도 걸려 있지 않아 이름을 바꿔도 파급이 없었다. 다만
  기용 기록은 옮겨야 하므로 이름 이사표에 넣었다

같은 종류의 오매칭이 더 있는지 시리즈로 훑었다. 어긋나는 148 건 가운데 147 건은
공식이 상위 시리즈명을 쓰기 때문이었고(08MS · 0080 · 0083 → "기동전사 건담"),
**진짜로 어긋난 것은 제스터 건담 하나뿐이었다.**

한편 `id-map.json` 이 788 장 시절 것이라 낡아 있었다. 다시 생성하니 짝지어진
기체가 466 에서 470 으로 늘었고, `M1 어스트레이` 가 건담 태그를 새로 받았다.

검토서도 "지금 PLEDGE_MUL 을 너프하지 말고 먼저 재라" 고 했으므로, **재기만 하고
수치는 건드리지 않았다.**

### 곁가지 — 색을 공식에 맞추고, 공식 태그 넷을 인연 축으로 들였다

`구프 이그나이티드` 가 우리 자료에는 **적색** 인데 공식은 색을 말하지 않는다는
지적에서 시작했다. 짝지어진 470 기를 통째로 견주니 어긋나는 곳이 많았다.

문제는 공식이 색을 **넷으로만** 나눈다는 것이다.

| | 흰 | 빨강 | 검정 | 금 | 초록 | 파랑 |
|---|---|---|---|---|---|---|
| 공식 (전체 1237 기) | 330 | 172 | 127 | 30 | — | — |
| 우리 (765 기) | 80 | 29 | 21 | 6 | 34 | 22 |

그래서 공식을 그대로 베끼면 녹색·청색 56 기가 색을 잃고 `푸른 기체들` ·
`녹빛 기체들` 두 인연이 통째로 죽는다. **공식의 침묵을 두 가지로 갈라 읽었다.**

| 상황 | 처리 | 기수 |
|---|---|---|
| 공식이 색을 말한다 | 공식을 따른다 | 165 |
| 공식은 말이 없고 우리가 녹·청 | 우리 것을 지킨다 — 공식에는 말할 칸이 없다 | 31 |
| 공식은 말이 없고 우리가 백·적·흑·금 | 뗀다 — 말할 수 있었는데 안 했다 | 21 |

셋째 줄이 `구프` · `구프 커스텀` · `구프 이그나이티드` 다.
정면으로 갈린 27 기는 공식을 따랐다 — `셴롱 건담` 녹색→백색, `사이코 자쿠`
녹색→적색처럼 공식이 넷으로 뭉갠 흔적이 보이는 것도 있으나, 기준을 하나로
정한 이상 예외를 손으로 두지 않았다.

결과: **백색 80→137 · 흑색 21→56 · 적색 29→67 · 금색 6→8 · 녹색 34→29 · 청색 22→14**

#### 새 인연 축 넷

공식 태그 100 가지 가운데 작품을 가로지르고 우리가 따로 적을 까닭이 없는 넷을
그대로 걸쇠로 삼았다.

| 인연 | 태그 | 장수 | 작품 수 |
|---|---|---|---|
| 변형하는 것들 | 가변기 | 66 | 14 |
| 외눈의 무리 | 모노아이 | 122 | 20 |
| 사람 모양이 아닌 | 모빌아머 | 31 | 13 |
| 지휘하는 자리 | 지휘관기 | 17 | 14 |

`tag.json` 에 `gge_tag` 규칙 갈래를 더해 카드의 `tags` 칸을 곧바로 읽는다.
`mech.json` 에 새 칸을 만들지 않으므로 축을 하나 더 다는 일은 자료 한 줄이다.
다만 **GGE 에 짝이 없는 295 기에는 붙지 않는다** — 이 축들의 한계다.

#### 문턱을 다시 잡았다

색 자료가 두 배로 늘었으므로 옛 문턱은 헐거워졌다. 무작위로 여섯을 뽑았을 때
기대 장수로 견주어 다시 잡았다.

| 인연 | 장수 | 옛 문턱 | 새 문턱 |
|---|---|---|---|
| 붉은 기체들 | 29→67 | 2:26 3:56 4:92 | 3:32 4:66 5:104 |
| 백색 편대 | 80→137 | 4:55 5:100 | 5:60 6:110 |
| 검은 기체들 | 21→56 | 2:30 3:66 | 3:36 4:76 |

#### 재어 본 값

| | 이론 천장 | 실전 평균 | 실전 최대 | 한 조 평균 |
|---|---|---|---|---|
| 손대기 전 | 291.9 | 134.6 | 251.3 | 85.9 |
| 색만 맞추고 축 넷 (문턱 그대로) | 366.8 | 150.7 | 262.5 | 85.6 |
| 문턱까지 다시 잡고 | 343.7 | **131.0** | **231.7** | 85.9 |

문턱을 그대로 두면 태그 인연이 한 조 값의 1.76 배까지 치솟는다 — 검토서가
이미 "1.6 배는 지나치다" 고 짚었던 쪽으로 더 간다. 문턱을 다시 잡으니 1.53 배로,
**축이 넷 늘었는데 총량은 오히려 조금 줄었다.** 노린 대로다.

이론 천장만 291.9 에서 343.7 로 올랐다. 쌓을 수 있는 축이 넷 늘었으니 당연한
일이고, 실제로 닿는 값은 반대로 251.3 에서 231.7 로 내려갔다. **천장은 높아졌지만
실전에서 그 근처에 가기는 더 어려워졌다** — 검토서가 걱정한 것은 뒤쪽이다.
천장을 이루는 여섯은 백식 · 골드프레임 아마츠 미나 · 리본즈 건담 · 이지스 ·
Z건담 · ZZ건담 이고, 금빛 기체 48 · 건담이라는 이름 135 · 주역의 자리 80 ·
숙적들 80 · 변형하는 것들 78 · 지휘하는 자리 70 이 한꺼번에 맺힌다.

승률은 흔들리지 않았다. 같은 씨앗 200 판씩이다.

| 적장 | 손대기 전 | 뒤 | 차 |
|---|---|---|---|
| 신참 | 87.0% | 87.5% | +0.5 |
| 숙련 | 54.5% | 49.5% | −5.0 |
| 에이스 | 27.5% | 29.5% | +2.0 |

200 판의 노이즈 바닥이 1σ ≈ 3.3%p 이므로 **셋 다 읽을 수 있는 차이가 아니다.**
아군 평균점도 882/884/875 에서 875/877/872 로 사실상 그대로다.

#### 곁가지의 곁가지 — 제스터 건담이 다시 어긋났다

첫 실행이 `제스터 건담` 에 `gundam: false` 를 도로 찍었다. 카드에 예전의 잘못된
짝(`1125002300`)이 남아 있었기 때문이다. `id-map.overrides.json` 에 `null` 은
적어 두었으나 `build-data.py` 를 돌리지 않아 카드까지 내려오지 않은 상태였다.
**순서가 있다 — `build-data.py` 로 짝을 정리한 다음에 `build-gge-tags.py` 를
돌려야 한다.** 그 김에 밀려 있던 짝 넷(`엘메다` · `지라` · `제이달스` · `제노`)과
`건담 AGE-1 플랫` 의 잘못된 형식번호도 함께 바로잡혀 짝이 466 에서 470 이 되었다.

`build-gge-tags.py` 는 읽는 곳도 옮겼다. 25MB 짜리 `.cache/` 대신 카드의 `tags`
칸을 읽는다 — 같은 자료를 `build-data.py` 가 옮겨 적어 저장소에 함께 넣어 두므로,
갓 받은 저장소에서도 그대로 돌아간다. 두 곳이 한 기도 어긋나지 않는 것은 확인했다.

### 곁가지 — 공식 이름과 대조해 보았다

"스로네" 하나가 형제 둘과 어긋난 것을 사람이 눈으로 찾아냈다. 같은 것이 더
있는지 보려고, GGE 에 짝이 있는 카드의 이름을 공식 이름과 통째로 견주었다.

| | 짝 | 이름이 같다 | 다르다 |
|---|---|---|---|
| 기체 | 470 | 365 (78%) | 105 |
| 파일럿 | 133 | 120 (90%) | 13 |

다른 105 건을 열어 보니 대부분은 **일부러 다르게 적은 것**이었다.

- 파일럿 12 건은 전부 줄임말이다 — 아무로 · 샤아 · 카미유. 판에 짧게 나와야 한다
- 공식이 제 로스터 안에서 헷갈리지 않으려 붙인 꼬리표 — `(선더볼트 Ver.)`
  `(지온 잔당군)` `(0085)`. 우리 로스터에는 그 짝이 없으니 붙일 까닭이 없다
- 기호를 한글로 편 것 — `ν건담` → 뉴 건담, `The O` → 디 오, `α 아질` → 알파 아질
- 로마숫자를 아스키로 적은 것 — `Ⅱ` → `II`

**진짜 잘못은 이름이 아니라 짝이었다.** 이름을 접어 보는 것으로는 못 잡고,
"한 공식 유닛에 카드가 둘 이상 붙었는가" 로 보니 세 묶음 열 장이 나왔다.

| 카드 | 붙어 있던 곳 | 붙어야 할 곳 |
|---|---|---|
| 건담 AGE-1 플랫 · 스팔로 · 타이터스 | 전부 `건담 AGE-1 노멀` | 각자의 유닛이 따로 있다 |
| 건담 AGE-1 그랜서 | `건담 AGE-1 노멀` | GGE 에 없다 |
| 건담 AGE-2 더블 불릿 | `건담 AGE-2 노멀` | `건담 AGE-2더블 불릿` |
| 건담 AGE-2 다크 하운드 | `건담 AGE-2 노멀` | GGE 에 없다 |
| M1 어스트레이 (SHRIKE 장비형) | `M1 어스트레이` | `M1어스트레이 (슈라이크 장비)` |

짝이 틀리면 레어도 · 지형 적성 · 태그가 통째로 남의 것이 되고, 태그를 근거로
찍는 색과 건담 표까지 따라 틀린다. 여덟 장이 그 상태였다.

그 가운데 **`건담 AGE-1 플랫` 은 내가 망가뜨린 것이다.** cc2c0ae 에서는 제
유닛(1400008110)에 제대로 붙어 있었는데, 색 작업 중에 `build-data.py` 를 돌리면서
낡은 id-map 이 그대로 실렸다. 형식번호도 `AGE-1F` 에서 `AGE-1` 로 밀렸다.

`id-map.overrides.json` 에 여덟 줄을 적어 갈랐다. 다시 세우니 겹치는 묶음이
하나도 남지 않았다.

이름 대조는 검사로 두지 않았다. 105 건 가운데 고칠 것이 0 건이라 — 걸리는 것이
전부 일부러 그런 것이면 아무도 안 보게 된다. 대신 `roster.shared_gge()` 를 두어
짝이 겹치는 것만 짚게 했다. 이쪽은 걸리면 전부 잘못이다.

**아직 사람이 정할 몫으로 남긴 것** — 표기가 안에서 흔들리는 자리다. 공식은
어느 쪽이든 하나로 통일되어 있으니 근거는 있으나, 이름을 바꾸면 그림 파일까지
따라가야 해서 물어보고 하는 것이 낫다.

| 우리가 두 가지로 적고 있다 | 공식 |
|---|---|
| 건담 아스트레이 … / M1 어스트레이 | 어스트레이 |
| 아헤드 / 어헤드 뇌양자파 대응형 | 어헤드 |
| 아프사라스 III / 압사라스II | 압사라스 |
| 자쿠II · 톨기스 II (사이띄개가 있다 없다) | — |
