# 로스터 — 자료가 어디 있고 무엇이 무엇을 만드나

카드 자료가 어디 있는지, 어느 칸이 누구 것인지, 카드와 초상을 새로 넣을 때
무엇을 다시 돌려야 하는지를 적어 둔다. 뒤쪽에 공식 사이트 대조 현황이 있다.

목록 대조는 `kr.gundam-official.com/series/[코드]/mecha` 를 받아 우리 목록과
맞춰 보는 식이다. 예전에는 자바스크립트 렌더링 페이지라 손으로 옮겨 적었는데,
Firecrawl 이 렌더링해 주므로 이제는 받아서 자동으로 대조한다. 받아 둔 목록은
`official/mecha/`, 대조는 `build-official.py`, 카드 생성은 `build-roster.py` 가
맡는다. 크레딧을 들여 받은 자료라 캐시가 아니라 저장소에 둔다.

## 자료가 어디 있나

카드 자료의 원본은 **`data/*.json`** 이다. 예전에는 `play.html` 안에 자바스크립트
배열로 박혀 있었는데, 카드 하나의 정보가 `MECH`·`WEIGHT`·`LORE`·`TAG_COLOR`·
`TAG_ROLE`·`GUNDAM_EXTRA` 여섯 군데로 흩어져 이름으로만 이어져 있었다. 그래서
표기가 한 글자 갈리면 같은 기체가 두 장이 됐다. 지금은 카드 하나가 레코드 하나다.

| 파일 | 내용 |
|---|---|
| `data/mech.json` | 기체 765. 능력치·세력·시리즈에 형식번호(`models`)·G 제네레이션 id(`gge`)·레어도·지형 적성·분류 태그, 드래프트 비중·태그·한 줄 소개까지 한 레코드에 |
| `data/pilot.json` | 파일럿 210. `gge`·레어도·분류 태그 포함 |
| `data/ship.json` · `crew.json` | 모함 79 · 지휘관 33 |
| `data/bond.json` | 전용기 연대 327 쌍 |
| `data/combo.json` | 연대·혈연·악연 164 |
| `data/img.json` | 초상 파일 목록. `register-images.py` 가 저장소의 `*.webp` 를 훑어 없는 것만 더한다 |
| `data/series.json` | 시리즈 이름·정규화 단위·진영 색 |
| `data/style.json` | 화풍 열하나. 초상 파일 이름의 화풍 토막과 화면에 뜨는 이름이 여기서 나온다 |
| `data/field.json` | 전장 넷과 뽑힐 비율, 지형 등급별 감점과 화면에 쓸 말 |
| `data/rule.json` | 판 진행 — 라운드마다 무엇을 뽑는지, 한 팩 장수, 정원 웃돈 기준 |
| `data/tag.json` | 태그를 무엇에서 뽑을지(derive)와 태그로 걸리는 인연 열둘(combos) |

읽고 쓰는 길은 `roster.py` 하나다. 스크립트마다 정규식으로 뜯어 읽던 것을
없앴다. 게임 규칙 코드는 아직 자리번호로 읽으므로(`m[7]` 은 계열) `roster.rows()`
가 예전 배열 꼴로 되돌려 준다.

`play.html` 은 실행할 때 `data/` 를 `fetch` 로 읽는다. 정적 호스팅 그대로지만
`file://` 로 열면 브라우저가 막으므로 `python3 -m http.server` 로 띄워야 한다.
`dex.html` 은 만들 때 자료를 박아 넣으므로 파일 하나로 열린다.

### 형식번호

`models` 는 두 곳에서 온다 — 공식 사이트 상세(`official/pilot/relations.json`)와
소샤지 G 제네레이션 API(`id-map.json`). 엇갈리면 공식을 따르고, 공식이 여러 카드를
한 항목에 몰아 적은 자리만 `data-overrides.json` 에 사람이 적어 둔다. 765 기 중
502 기(64%)가 갖고 있다. 나머지는 어느 쪽에도 안 실린 단역·양산기다.

형식번호가 있으면 이름이 갈려도 같은 기체인지 드러난다. 실제로 이걸로
`V건담`/`빅토리 건담`(둘 다 LM312V04)을 잡아냈다.

### 바깥에서 가져온 칸

`build-data.py` 가 소샤지 API 캐시에서 카드로 옮겨 붙인다. 게임 규칙은 아직
쓰지 않는다 — 자리부터 만들어 두고 무엇이 쓸 만한지 보는 단계다.

| 칸 | 뜻 | 채워진 정도 |
|---|---|---|
| `rarity` | G 제네레이션 레어도 1~5 | 기체 466 · 인물 133 |
| `terrain` | 지형 적성. 우주·대기권·지상·수상·수중, **1 부적합 · 2 보통 · 3 적합** | 기체 765 (아래) |
| `terrain_src` | 그 지형값이 어디서 왔는지. 없으면 소샤지 원본 | 기체 322 |
| `tags` | 분류 태그 | 기체 460 · 인물 131 |
| `large` | 두 칸을 차지하는 대형기 | 기체 14 |

태그는 100 종 가운데 골라 남긴다. 시리즈·진영 태그는 `series` 와 `factions` 로
이미 갖고 있어 겹치고, 돌파력·일격필살 같은 것은 G 제네레이션 전투 안에서만
뜻이 있어 여기로 가져와도 가리키는 데가 없다. 남기는 목록은
`build-data.TAG_MECH` / `TAG_PILOT` 에 있다.

지형 적성은 원작의 물리가 아니라 **게임의 밸런스 표**다. 건탱크가 우주 3 이고
윙 건담 제로가 수중 3 이다. 경향은 맞지만 낱개로는 믿을 값이 아니다.

### 지형 적성을 어디서 얻나

소샤지에 실린 466 기는 그쪽 값을 쓰고, 나머지 322 기는 `build-data.fill_terrain`
이 메운다 — 형식번호가 같은 카드(5) → 이름 계보가 이어지는 카드(25) →
계열 중앙값(285). 지어낸 값에는 `terrain_src` 를 남겨 아는 값과 구분한다.

계열 중앙값이 그럴듯한 것은 실제로 갈리기 때문이다.

| 계열 | 표본 | 중앙값 |
|---|---|---|
| 양산·시작기·사이코뮤·아르비 | 236·109·34·12 | 우주 3 · 대기권 **1** · 지상 3 · 수중 2 |
| GN드라이브·핵동력·GUND | 25·18·7 | 우주 3 · 대기권 **3** · 지상 3 · 수중 2 |
| 모빌트레이스 | 18 | 우주 3 · 대기권 1 · 지상 3 · 수중 **3** |

UC 계열 기체는 날지 못하고 이후 작품 기체는 난다는 것이 수치에 그대로 남아
있다. 중앙값이 뻔히 틀리는 자리(V2건담은 미노프스키 드라이브로 나는데 표본이
둘뿐이라 대기권 1 이 나왔다)는 `data-overrides.json` 의 `terrain` 에 이유와 함께
적는다. 지금 일곱 자리.

## 어느 파일이 무엇을 하나

### 화면 (사람이 여는 것)

| 파일 | 하는 일 |
|---|---|
| `index.html` | 들머리. 아래 넷으로 가는 링크만 있다 |
| `play.html` | **드래프트 게임.** 실행할 때 `data/` 를 읽는다. 도감 · 공적 · 통계도 이 안에 있다 |
| `dex.html` | **도감.** 카드를 훑고 초상 작업 현황을 본다. `build-dex.py` 가 만드는 **생성물이라 손으로 고치지 않는다** |
| `prompt.html` | **초상 프롬프트 툴킷.** 의인화 · 콜라주 · 단일 컷 셋을 만든다. 화면에 판 번호와 패치 노트가 있다 |
| `sam.html` | 삼국지 드래프트. 건담보다 먼저, git 을 쓰기 전에 만든 통짜 파일이다 → `TRY_SAMGUK.md` |
| `image-list.html` · `mech-remaining.html` | 초상 작업 목록 · 남은 기체. 자료를 박아 만든 옛 화면으로, 도감이 같은 일을 한다 |

### 자료 — `data/` (게임과 화면이 실행할 때 읽는 것)

| 파일 | 하는 일 |
|---|---|
| `mech.json` | 기체 765. 능력치 · 세력 · 시리즈 · 형식번호 · G 제네레이션 id · 레어도 · 지형 · 태그 |
| `pilot.json` · `ship.json` · `crew.json` | 파일럿 210 · 모함 79 · 지휘관 33 |
| `bond.json` | 전용기 — `"파일럿\|기체"` → 보정치 0.12~0.22 |
| `combo.json` | 인연 · 혈연 · 악연. `{n, m, t, k}` 꼴 |
| `img.json` | 초상 파일 목록. 성별 · 화풍별로 나뉜다 |
| `series.json` | 시리즈 이름 · 정규화 단위 · 진영 색 · 차례 |
| `style.json` | 화풍 열하나 |
| `field.json` | 전장과 뽑힐 비율 · 지형 등급 감점 |
| `rule.json` | 판 진행 — 라운드 차례 · 팩 장수 |
| `tag.json` | 태그를 뽑는 규칙과 태그로 걸리는 인연 |

### 만드는 스크립트

| 파일 | 하는 일 |
|---|---|
| `roster.py` | **자료를 읽고 쓰는 유일한 길.** 다른 스크립트는 전부 이걸 거친다 |
| `build-roster.py` | 공식 사이트 대조 결과를 카드로 만들어 `data/` 에 넣는다 |
| `build-official.py` | 공식 사이트 목록 ↔ 저장소 로스터 대조 → `official-diff.json` |
| `build-idmap.py` | G 제네레이션 API 와 카드를 짝지어 `id-map.json` 을 만든다 |
| `build-data.py` | 형식번호 · id · 레어도 · 지형 · 태그를 카드에 붙인다. `--check` · `--report` |
| `build-bond.py` | 공식 탑승 관계에서 전용기 후보를 뽑는다 → `bond-candidates.json` |
| `build-dex.py` | `play.html` 에서 코드를 떼어다 `dex.html` 을 만든다 |
| `register-images.py` | 저장소의 `*.webp` 를 `data/img.json` 에 등록한다. `--check` · `--prune` |
| `gundam_match.py` | 이름 대조 공용 규칙. 세 출처가 같은 기체를 다르게 적어서 한곳에 모았다 |

### 사람이 손으로 적는 것

| 파일 | 하는 일 |
|---|---|
| `data-overrides.json` | 자동으로 고른 형식번호 · 지형이 틀린 자리. `why` 에 근거를 적는다 |
| `roster-overrides.json` | `build-roster.py` 가 규칙으로 매긴 능력치를 덮어쓴다 |
| `id-map.overrides.json` | 자동 대조로 못 잡은 API id 짝 |
| `official-slugs.json` | 시리즈 코드 ↔ 공식 사이트 슬러그 |

### 중간 생성물 (사람이 읽고 판단하라고 만든 것)

| 파일 | 하는 일 |
|---|---|
| `official-diff.json` | 공식에는 있고 저장소에 없는 것. `near` 는 같은 대상인지 사람이 봐야 한다 |
| `id-map.json` | 카드 ↔ API id. `review` 는 확인이 필요한 짝 |
| `bond-candidates.json` | 전용기 후보. 값은 밸런스 판단이라 비워 둔다 |
| `toolkit-data.json` | 툴킷의 '기록 내보내기' 결과. 기체별 화풍 · 진행 상황 |

### 그 밖에

| 파일 | 하는 일 |
|---|---|
| `.github/workflows/register-images.yml` | `main` 에 `*.webp` 가 올라오면 `register-images.py` 를 돌려 `img.json` 을 갱신하고 되커밋 |
| `data-samguk/` | 주제를 갈아 끼워 본 시험 자료 30 장. `play.html?set=samguk` |
| `official/` | 공식 사이트에서 받아 둔 원본 목록 |
| `.cache/soshage/` | G 제네레이션 API 응답 캐시 |

## 누가 무엇을 갖나

셋이 붙어 있다. 같은 파일을 둘이 건드리면 충돌이 기본값이 되므로 파일로 나눈다.

| | 갖는 것 |
|---|---|
| Claude (이 세션) | 코드 전부 — `prompt.html` · `play.html` · `build-dex.py` · `data/*.json` · 문서 |
| 이미지 세션 | `*.webp` · `data/img.json` · `toolkit-data.json` |
| 검토자 | **아무것도 쓰지 않는다.** 읽고 지적만 한다 → `REVIEW.md` |

`dex.html` 은 `build-dex.py` 가 만드는 생성물이라 손으로 고치지 않는다.
`prompt.html` 의 판 번호도 한 곳에서만 올린다 — 둘이 같은 번호를 쓰면 반드시 겹친다.

## 한 자리에서만 적는 것들

같은 값을 두 군데 적어 두면 언젠가 갈린다. 아래는 그래서 한 자리로 몬 것들이다.

| 값 | 사는 곳 | 화면은 어떻게 얻나 |
|---|---|---|
| 능력치 이름 (출력·화력…) | `data/*.json` 머리말의 `stats` | 불러올 때 `STAT_LABEL` 에 채운다. 화면에 또 적지 않는다 |
| 카드 종류 넷 | `play.html` 의 `KIND` · `build-dex.py` 의 `KIND` | 받아올 파일 이름, 능력치 이름표, 도감 탭이 다 여기서 갈라진다 |
| 전장·지형 감점 | `data/field.json` | `FIELDS`·`TERR`·`TERR_WORD` 로 채운다 |
| 판 진행 차례 | `data/rule.json` | `SCHEDULE`·`PACKN`·`CAP_BONUS` 로 채운다 |
| 태그 규칙·태그 인연 | `data/tag.json` | `TAG_RULE`·`TCOMBO` 로 채운다 |
| 진영 색 | `data/series.json` 의 `faction_color` | `FAC` 로 채운다 |

자료 묶음을 통째로 갈아 끼우려면 `play.html?set=이름` 으로 `data-이름/` 을 읽는다.
삼국지 30 장으로 실제로 해 본 기록이 `TRY_SAMGUK.md` 에 있다 — 코드는 열아홉 줄
고쳤고, 남은 결합은 태그 인연·화면 낱말·업적 셋이다.

카드 종류를 하나 더 만들면 `KIND` 에 `["이름","파일이름"]` 을 더하고 `data/파일이름.json`
을 두면 된다 — 받아오기·능력치 이름표·도감 탭이 따라온다. 다만 카드 배열
(`MECH`·`PILOT`…)과 자리번호로 읽는 규칙 함수(`mp`·`pp`·`sp`·`cp`)는 아직 종류마다
손으로 적는다. 거기까지 데이터로 빼는 것은 두 번째 주제가 정해진 뒤에 할 일이다.

## 전장 — 지형 적성이 게임에 들어가는 자리

`play.html` 은 판을 시작할 때 전장을 하나 뽑는다(`reset()` → `rollField()`).
기체의 `terrain` 이 그 전장에서 싱크에 더해진다 — **적합 0 · 보통 −.06 ·
부적합 −.18**. 기질(±.20)·감응(−.20~+.25)·전용기(+.12~.22)와 같은 눈금이다.

적합에 웃돈을 주지 않고 못 맞춘 쪽만 깎는다. 대부분의 기체가 우주·지상에
적합해서, 보너스로 주면 점수만 전반적으로 오르고 고르는 재미는 안 생긴다.

| 전장 | 뽑힐 확률 | 비중 0.4 이상 385 기 기준 | 평균 |
|---|---|---|---|
| 우주 | 34% | 적합 363 · 보통 7 · 부적합 15 | −.008 |
| 지상 | 34% | 적합 357 · 보통 8 · 부적합 20 | −.011 |
| 수중 | 20% | 적합 79 · 보통 276 · 부적합 30 | −.057 |
| 대기권 | 12% | 적합 117 · 보통 11 · 부적합 257 | −.122 |

수상은 뽑지 않는다. 765 기 중 적합이 열여섯 뿐이라 판이 서지 않는다.

셋 중 둘은 우주나 지상이라 예전과 거의 같이 굴러가고, 수중은 다 같이 굼뜬
판이, 대기권은 날 수 있는 기체를 찾는 판이 된다. 어려운 전장을 드물게 둔 것은
제약이 매 판 오면 제약이 아니라 세금이 되기 때문이다. 한 장을 고를 때
지형이 갈리는 폭은 짝 점수의 18% 로, 전용기 인연 하나와 비슷하다.

### 대조 결과

`python3 build-data.py --report` 가 새 태그와 손으로 적어 둔 값이 어긋나는 곳을
짚어 준다. 고치지는 않는다 — 소샤지 태그는 게임이 편하려고 거칠게 나눈 것이고
(색이 흰·빨강·검정·금 넷뿐이다), 반대로 손으로 적은 쪽이 틀린 적도 있다.

지금 나오는 것: 색 어긋남 27 · 색 빈칸 138 · 역할 어긋남 1 · 역할 빈칸 76 ·
계열 어긋남 10 · 감응 등급 0 인데 게임은 감응자로 보는 인물 6.

계열 어긋남에는 우리 자료 안에서 앞뒤가 안 맞는 데도 섞여 있다. `ZZ건담` 은
사이코뮤인데 `강화형 ZZ건담` 과 `풀 아머 ZZ건담` 은 시작기로 적혀 있다.

## 무엇이 무엇을 만드나

원본은 `data/` 하나이고 화면 셋이 거기서 나온다. 셋이 자료를 받는 방식이 서로
다르니 어느 것을 다시 만들어야 하는지도 다르다.

```
official/ ─┐
           ├─→ build-roster · build-idmap · build-data ─→ data/*.json
소샤지 API ─┘                                                 │
                                                             ↓ 실행할 때 fetch
                                            play.html · dex.html · prompt.html
```

셋 다 실행할 때 `data/` 를 받는다. 어느 것도 자료 사본을 들고 있지 않으므로
**자료가 바뀌어도 다시 만들 파일이 없다.** 화면 코드를 고칠 때만 `dex.html` 을
`build-dex.py` 로 다시 만든다(도감은 통째로 생성물이다). `play.html` 과
`prompt.html` 은 손으로 고치는 파일이다.

`register-images.py` 는 저장소의 `*.webp` 를 `data/img.json` 에 등록한다.
`play.html` 과 `dex.html` 이 그걸 읽는다.

`file://` 로는 셋 다 안 열린다. 브라우저가 `fetch` 를 막기 때문이고, 그때는
왜 안 되는지와 어떻게 띄우는지를 화면에 적어 준다. GitHub Pages 로 보는 데는
아무 문제가 없다.

### prompt.html 을 고쳤으면 판을 올린다

`prompt.html` 에만 화면에 뜨는 판 번호와 패치 노트가 있다. **세 곳을 같이 고친다.**

```
<title>Gundam Prompt Toolkit v9.9</title>      머리
<div class="ver-badge"> … <b>v9.9</b> …        배지
<details id="verBox">  안의 맨 위에 항목 추가   패치 노트
```

문구 한 줄을 바꾸든 파라미터를 하나 더하든 올린다. 결과가 달라지는 자리라
"저번과 왜 다르지" 를 되짚을 근거가 그 목록뿐이다. 실제로 v9.5 이후 열 번을
고치고도 안 올려서, 나중에 v9.6~v9.9 를 한꺼번에 되짚어 채워야 했다.

끝자리를 올리는 기준은 묶음 하나다 — 고친 곳이 여럿이어도 한 가지 이야기면 한 판이다.
`play.html`·`dex.html` 에는 이 표시가 없다. 거기는 git 기록만 본다.

### prompt.html 이 받는 것

툴킷은 `data/mech.json` 과 `data/series.json` 둘만 받아 필요한 꼴로 만든다.

| 이름 | 내용 | 툴킷이 쓰는 데 |
|---|---|---|
| `MECH` | `[이름, 시리즈[]]` | 시리즈 고르개·기체 고르개 |
| `MECH_META` | 능력치·세력·성격·계열·시리즈 | 프롬프트 추천과 본문에 얹는다 |
| `SER_NAME` | 시리즈 코드 → 한글 이름 | 고르개 표시 |
| `SERIES_ORDER` | 고르개에 뜨는 차례 | `series.json` 의 `order` |

`MECH_META` 는 툴킷이 실제로 읽는 열만 만든다. 형식번호도 한 번 넣어 봤으나
프롬프트를 만드는 데 쓰이는 데가 없어 뺐다 — 1 만 자를 차지했다.

고르개 차례(`series.json` 의 `order`)는 손으로 정한 것이라 그대로 두고, 새
시리즈 코드만 `build-data.py` 가 뒤에 붙인다.

예전에는 이 넷이 통째로 박혀 있었고 `build-prompt.py` 가 게임 쪽 자료에 맞춰
갈아끼웠다. 47 만 자 파일에 정규식으로 수술하는 방식이라 블록 앞뒤 모양이
바뀌면 못 찾았고, 툴킷을 고칠 때마다 자료가 뒤처졌다. 그 스크립트는 이제 없다.

### 반대로 prompt.html 이 내놓는 것

`toolkit-data.json` 은 흐름이 거꾸로다. 툴킷에서 '기록 내보내기' 로 뽑아
저장소에 커밋하면, **도감이 그걸 실행 중에 읽어** 어느 기체가 어떤 화풍으로
끝났는지 보여 준다. 사람이 덮어써야 하는 유일한 자료 파일이다.

`image-list.html` 은 그 반대편이다. `build-dex.py` 가 여기서 프롬프트와 비고를
뽑아 도감에 넣는다. 카드 이름이 바뀌면 `play.html` 의 `RENAME_MAP` 을 거쳐
따라간다. 지금은 `toolkit-data.json` 8 기 · `image-list.html` 529 행 모두
살아 있는 카드를 가리킨다.

카드 이름을 바꾸면 이 둘과 `data/img.json` 의 파일 이름이 같이 어긋난다.
이름은 웬만하면 바꾸지 않는 편이 낫고, 바꿔야 하면 `RENAME_MAP` 에 넣는다.

## 카드를 새로 넣을 때 — 어느 칸이 누구 것인가

`data/*.json` 의 칸은 **손이 임자인 것**과 **`build-data.py` 가 임자인 것**으로
갈린다. 뒤쪽에 손으로 적어 넣으면 다음 실행 때 지워진다. 지울 때 무엇을
지웠는지 찍어 주기는 하지만, 처음부터 제자리에 넣는 편이 낫다.

| 칸 | 임자 | 새 카드에 넣을 때 |
|---|---|---|
| `name` `factions` `stats` `temper` `system` `series` | 손 | 카드에 직접 |
| `weight` `color` `role` `gundam` `lore` | 손 | 카드에 직접 |
| `models` | build-data | **`data-overrides.json` 의 `models`** 에 |
| `terrain` | build-data | 지어낸 값이 틀리면 **`data-overrides.json` 의 `terrain`** 에 |
| `gge` `rarity` `tags` `large` `terrain_src` | build-data | **적지 않는다.** G 제네레이션에 실린 기체에만 있는 값이다 |

`models` 를 카드에 직접 적으면 사라지는 것은, 그 칸의 값을 공식 사이트와
소샤지에서 다시 만들기 때문이다. 바깥 자료에 없는 기체의 형식번호는
`data-overrides.json` 에 적어야 남는다. 지형도 같다 — 안 적으면 계열
중앙값으로 지어낸 값이 들어간다(`terrain_src` 가 그걸 알려 준다).

없어도 되는 칸이다. `models` 가 없으면 도감에 형식 줄이 안 뜨고 이름이
갈린 중복을 못 잡을 뿐, 게임은 그대로 돈다.

## 초상을 새로 넣을 때

파일을 저장소에 올리고 `python3 register-images.py` 를 돌린다. 카드 이름의
공백은 밑줄로 써도 된다.

```
<카드 이름>_f.webp            여성 초상
<카드 이름>_m.webp            남성 초상
<카드 이름>_f_casual3.webp    여성 일상컷 3
<카드 이름>_m_casual3.webp    남성 일상컷 3
<카드 이름>_f_extra1.webp     여성 특별컷 1
```

**일상컷·특별컷도 초상처럼 성별을 나눈다.** 도감에서 남/여를 바꾸면 그 성별의
컷만 뜬다. 성별 표시가 없는 예전 이름(`<카드 이름>_casual3.webp`)은 여성으로
친다 — 지금 있는 것이 전부 여성이라 358 장의 이름을 다시 붙이는 대신 규칙
한 줄로 받는다. 앞으로 올리는 것은 양쪽 다 표시를 붙이는 편이 낫다.

### 같은 카드를 다른 화풍으로 뽑았을 때

카드 이름과 칸 사이에 화풍 key 를 끼운다.

```
건이지_f.webp                    기본 몫  — 지금까지 쓰던 그대로
건이지_glossy_kr_game_f.webp     화풍 몫  — byStyle.glossy_kr_game 으로 들어간다
건이지_glossy_kr_game_casual1.webp
```

쓸 수 있는 key 는 `data/style.json` 에 적힌 열하나뿐이다. 오타가 나면 그 토막이
카드 이름의 일부로 읽혀 "그런 카드가 없다"로 걸린다 — 엉뚱한 화풍 칸이 조용히
생기는 것보다 낫다고 봐서 일부러 통과시키지 않는다.

기본 몫을 비워 두고 화풍 몫만 채워도 된다. 게임은 **가진 그림 중에서** 고르지,
기본 몫이 있어야만 뜨는 게 아니다.

얼굴 좌표(`face`)는 화풍 몫에 없으면 기본 몫 것을 빌려 쓴다. 같은 인물을 같은
구도로 뽑았으면 대개 맞고, 어긋나면 그 화풍 몫에만 `face` 를 적으면 된다.

**이미 적힌 항목과 얼굴 좌표(`face`)는 건드리지 않는다.** 없는 것만 더하므로
손으로 넣어 둔 것과 어긋나지 않는다. `face` 는 파일 이름에서 알 수 없어
비어 있으면 게임이 기본값 `[.34,.03,.30]` 을 쓴다 — 뜨기는 뜨고, 잘린 자리가
어색하면 `data/img.json` 에서 사람이 손본다.

`play.html` 은 실행 중에도 GitHub 파일 목록을 받아 같은 규칙을 한 번 더
돌린다(`mergeRepoFiles`). 화풍 토막도 같이 알아본다. 그래서 `img.json` 에 안 적혀 있어도 화면에는 뜬다.
다만 그쪽은 얼굴 좌표를 못 넣고 목록을 못 받으면 그만이라, 스크립트로
파일에 굳혀 두는 편이 낫다. 그 함수는 빈 자리만 채우고 기존 값을 덮지 않는다.

**손으로 돌리지 않아도 된다.** `.github/workflows/register-images.yml` 이
`main` 에 `*.webp` 가 올라오면 이 스크립트를 돌려 `data/img.json` 을 갱신하고
되커밋한다. 파일이 없어진 항목까지 지우려면 Actions 에서 손으로 돌리면서
`prune` 을 켠다.

등록 못 한 파일이 있어도 스크립트는 성공으로 끝난다 — 아홉 장 중 한 장이
카드 이름과 안 맞아 빠져도 워크플로는 초록이다. 그래서 Actions 안에서 돌 때는
경고와 실행 요약을 남긴다. 초상을 올린 뒤에는 실행 요약의 '새로 등록' 숫자가
올린 장수와 맞는지만 보면 된다.

## 화풍이 게임에 나오는 자리

한 카드의 그림은 기본 몫(`IMG[이름]`)과 화풍 몫(`IMG[이름].byStyle[화풍]`)에
나뉘어 있고, 둘의 속은 같은 꼴이다. `play.html` 은 판을 시작할 때 카드마다
**가진 몫 중 하나를 무작위로 골라**(`rollArt`) 그 판 내내 그것으로 그린다.
한 판 안에서 카드마다 화풍이 달라도 상관없다는 전제다. 같은 판에서 화풍이
바뀌지 않는 이유는 패를 다시 그릴 때마다 그림이 갈리면 눈이 어지러워서다.

| 함수 | 하는 일 |
|---|---|
| `artBuckets(이름)` | 그 카드가 실제로 그림을 가진 몫 전부 |
| `rollArt()` | 판마다 한 번. 카드별로 하나씩 뽑아 `ASEED` 에 넣는다 |
| `artBucket(이름)` | 이번 판에 쓸 몫. 뽑힌 게 없으면 첫째 몫 |

카드 상세를 열면 지금 판의 화풍만 크게 뜨고, 갖고 있는 일상컷·특별컷은
화풍 이름을 달아 전부 보여준다.

`dex.html` 은 이와 별개로 화풍 탭과 시트 안 화풍 넘기기를 이미 갖고 있다
(`styleBucket`). 도감은 고르는 쪽, 게임은 섞는 쪽이다.

**아직 안 한 것** — 판마다 화풍을 지정하거나 한 화풍으로 통일하는 설정.
지금은 무조건 섞는다. 화풍 몫이 한 카드(`건이지`)에만 있어서 고르는 설정을
넣어봐야 화면이 안 바뀌기 때문이고, 그림이 쌓이면 `rollArt` 에 조건 한 줄을
더하는 일이다.

## 영문으로 남겨 둔 이름

원문이 영문인 것은 한글로 옮기지 않는다. 한국 자료도 그대로 쓴다.

```
GN-X · GN-XIII · GN-X IV · GFreD · GQuuuuuuX · GUNDVÖLVA
```

아래 둘은 무엇을 가리키는지 못 밝혔다. 잘못 옮기면 되돌리기 번거로워서 그냥 뒀다.

```
Blue        SEED FREEDOM 공식 목록 끝에 Birdy 와 나란히 있다. 기체인지도 불확실
Toro Hachi  유니콘 [민간] 칸에서 하로 옆에 있다. 마스코트류로 보이나 원문 표기를 못 찾았다
```

## 사용법 (다음 세션에서)

1. `official-slugs.json` 에 시리즈 코드와 공식 사이트 슬러그를 넣는다
2. Firecrawl 로 `series/<슬러그>/mecha` 를 받아 `official/mecha/<코드>.json` 에 넣는다
   (`{"세력": ["이름", ...]}` 꼴. 이 환경에서 공식 사이트는 직접 접근이 막혀 있다)
3. `python3 build-official.py --report` 로 대조하고, `python3 build-roster.py` 로
   카드를 만든다. 카드는 `data/` 에 들어간다
4. `python3 build-idmap.py` 로 G 제네레이션 id 를 다시 받고,
   `python3 build-data.py` 로 형식번호와 id 를 카드에 붙인다
5. 초상을 올렸으면 `python3 register-images.py` 로 `data/img.json` 에 등록한다
6. 화면은 다시 만들 것이 없다. 셋 다 실행할 때 `data/` 를 읽는다.

도감 **코드**를 고쳤을 때만 `python3 build-dex.py play.html` 을 돌린다.
`gundam-dex-x.html` 로 나오므로 `mv gundam-dex-x.html dex.html` 로 옮겨야 한다.

## 로스터 대조 현황

### 완료 (대조 끝)

2026-09-05 부터는 공식 사이트 목록을 Firecrawl 로 직접 받아 대조한다.
`official/mecha/<코드>.json` 에 받아 둔 것을 `build-official.py` 가 읽어
저장소와 맞춰 본다. 손으로 옮겨 적을 일이 없어져서 28 개 시리즈를 한 번에
끝냈다. 지금 대조 결과는 추가 후보 0 — 공식 목록 기준으로 빠진 기체가 없다.

| 시리즈 | 코드 | 방식 | 결과 |
|---|---|---|---|
| 28 개 전 시리즈 | — | **공식 사이트 자동 대조** | 기체 516 · 함선 23 추가 (272→788, 43→66) |
| 시드 계열 함선 | 2026-09-06 | **기체에 섞여 있던 함선 정리** | 기체 19 장 빼고 함 13 장 추가 (788→769, 66→79) |
| 영문 이름 기체 | 2026-09-06 | **로마자 잔재 정리** | 이름 26 · 계열/능력치 7 · 중복 3 지움 (769→766). 남긴 여덟은 아래 |
| 弐式 표기 | 2026-09-06 | **이식 = TypeⅡ 중복** | 인피니트 저스티스 합침 (766→765) · SpecII 표기 통일 |

이전에 손으로 끝낸 것들(NT, GQuuuuuuX, 섬광의 하사웨이, 건담 00 극장판,
SEED ASTRAY 스타게이저)은 자동 대조에서도 추가 후보 0 으로 확인됐다.

### 대기

없다. 새 시리즈가 공식 사이트에 올라오면 `official-slugs.json` 에 슬러그를
넣고 그 시리즈의 `mecha` 페이지를 받아 `build-official.py` 를 돌리면 된다.

## 참고

- "하로" 같은 마스코트 로봇, 차량, 시설은 기체 로스터에서 제외 (`gundam_match.EXCLUDE_NAMES`)
- 함선은 제외가 아니라 SHIP 으로 돌린다 (`gundam_match.SHIP_NAMES` / `SHIP_SUFFIX`)
- 장비/사양 배리에이션도 이제는 다 넣는다. 빼는 대신 드래프트 등장 비중을 낮춘다
  (`WEIGHT` 0.15). 도감은 완비되고 팩에 잡히는 체감은 그대로다
- 스탯 4개는 공식 수치가 없어서 임의 배정이다. `build-roster.py` 가 특성별 평균
  (양산 65/67/65/66, 사이코뮤 90/88/82/78, 핵동력 92/89/83/91 …)에서 이름 해시만큼
  흔들어 매긴다. 같은 이름은 늘 같은 값이 나온다. 개별 조정은 `roster-overrides.json`
- soshage 수치를 옮겨 쓰려 했으나 212 기로 맞춰 보니 축끼리 상관이 0.27~0.54 로 낮고
  대응도 어긋나 쓰지 않았다. 게임 밸런스용 수치와 원작 인상 기준 수치는 다른 것을 잰다
- 같은 기체가 공식 표기와 저장소 표기로 갈려 두 장이 된 것 10 장을 걷어냈다.
  건담 NT-1 Alex↔건담 NT-1, 건담 칼리번↔건담 캘리번, 윙 건담 0↔윙 건담 제로 EW,
  건담 DX↔더블 엑스, 유니콘 건담 3호기 페넥스↔페넥스,
  리본즈 건담/리본즈 캐논↔리본즈 건담, 고트라탄↔고틀라탄, 리그 샷코↔리그 샤코, 샷코↔샤코,
  빅토리 건담↔V건담.
  손질해 둔 쪽(그림·설명·인연이 붙은 쪽)을 남기고 반입분을 지운 뒤
  `roster-overrides.json` 의 `skip` 과 `build-bond.ALIAS_MECH` 에 넣어 다시 안 생기게 했다.
  이름으로 찾을 때는 `gundam_match.norm` 으로 편 다음 자모로 풀어(NFD) 유사도를 잰다.
  글자 단위로만 재면 칼리번/캘리번 같은 한 글자 차이를 놓친다. 이름이 아예 다른
  V건담/빅토리 건담 은 그래도 못 잡는다 — 그건 형식번호가 잡았다
- 총 기체 수: 765, 함선 79 (2026-09-06 기준)
- 드래프트 비중: 기존 272 기가 1.0, 새 조연·에이스기 113 기가 0.4, 양산·배리에이션 403 기가 0.15.
  9 장 팩 기준으로 기존 272 기가 팩의 71% 를 차지한다
