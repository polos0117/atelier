# 이미지 업로드·도감 등록 규칙

이 문서는 `polos0117/atelier`에서 이미지를 다루는 AI와 작업자를 위한 필수 선행 문서다. 업로드 전, 이름 변경 전, 중단된 작업을 재개하기 전에 읽는다.

## 1. 반드시 지킬 원칙

- 기존 기본 자리의 이미지는 **화풍 미상**이다. 임의로 세미리얼 시네마틱 또는 같은 카드의 다른 화풍으로 재분류하지 않는다.
- 신규 이미지는 생성 당시 기록 또는 사용자가 확정한 화풍 키를 파일명에 넣는다.
- 이미지 외형을 보고 추정한 화풍과 실제 생성기 선택값은 구분한다. 추정만 있는 이미지는 분류표에 미확정으로 남기고 등록을 보류한다.
- 같은 카드라도 이미지마다 화풍·성별·인물·용도가 다를 수 있다. 카드 단위로 모든 파일에 같은 화풍을 일괄 부여하지 않는다.
- `toolkit-data.json.style`, 브라우저의 과거 `style`, 현재 `stylePreferences`는 업로드 파일의 화풍을 확정하는 증거가 아니다. 프롬프트 선택을 바꿔도 기존 이미지 분류는 바뀌지 않는다.
- 이미 등록된 이미지, 얼굴 좌표, 컷 배열, 다른 작업자의 변경을 보존한다. 파일 추가를 위해 기존 초상을 덮어쓰거나 검증을 완화하지 않는다.

## 2. 작업 전에 읽을 파일

| 파일 | 확인할 내용 |
|---|---|
| `data/mech.json`, `pilot.json`, `ship.json`, `crew.json` (모두 `data/` 아래) | 실제 카드 이름 |
| `data/style.json` | 허용된 화풍 키의 최신 목록 |
| `register-images.py` | 파일명 해석과 등록·검증 동작 |
| `data/img.json` | 기존 파일, 화풍별 자리, 성별, 컷 번호, 얼굴 좌표 |
| `.github/workflows/register-images.yml` | 자동 등록 실행 조건 |
| `gundam/README.md` | 화면 자산을 함께 수정할 때의 규칙 |

같은 세션에서 읽은 규칙·코드는 변경되지 않았다면 다시 읽지 않는다. 카드 데이터는 대상 카드가 속한 파일과 해당 항목 위주로 확인한다.

파일명·화풍 키·카드 이름은 기억으로 만들지 말고 현재 저장소와 대조한다. 이 문서와 코드가 다르면 먼저 차이를 확인한다.

## 3. 저장 경로와 파일명

이미지 파일은 저장소 루트의 **`img/` 바로 아래**에 둔다. `data/img.json`에는 `img/`를 제외한 파일명만 기록한다.

| 용도 | 신규 파일명 |
|---|---|
| 여성 의인화 초상 | `<카드명>_<화풍키>_f.webp` |
| 남성 의인화 초상 | `<카드명>_<화풍키>_m.webp` |
| 여성 일상컷 | `<카드명>_<화풍키>_f_casualN.webp` |
| 남성 일상컷 | `<카드명>_<화풍키>_m_casualN.webp` |
| 여성 특별컷 | `<카드명>_<화풍키>_f_extraN.webp` |
| 남성 특별컷 | `<카드명>_<화풍키>_m_extraN.webp` |

- 카드명의 공백은 공백 그대로 또는 밑줄로 표현할 수 있다. 둘은 같은 카드로 해석되므로 두 표기로 중복 업로드하지 않는다.
- 화풍 키는 `data/style.json`의 영문 키를 그대로 사용한다. 한글 표시명, 임의 약어, `unknown`을 신규 파일명에 넣지 않는다.
- `N`은 1 이상의 정수다. 번호는 해당 카드·화풍·성별·용도 안에서 기존 파일명을 확인해 정한다. 배열 길이만 보고 다음 번호를 정하지 않는다.
- 신규 일상·특별컷에도 성별을 명시한다. 성별 없는 예전 파일명은 기존 자료 호환용이다.
- `style-`로 시작하는 파일은 화풍 견본으로 등록 검사에서 제외된다. 카드 이미지가 검증을 피하도록 이 접두사를 사용하지 않는다.
- 현재 초상 슬롯은 카드·화풍·성별마다 하나다. 이미 초상이 있으면 추가 변형을 임의로 같은 경로에 덮거나 일상컷으로 위장하지 않는다. 교체 의도 또는 추가 저장 방식이 필요한지 확인한다.

예시: `건이지_cinematic_semi_real_f.webp`, `건이지_cinematic_semi_real_f_casual1.webp`.
예시의 카드명·화풍은 모든 작업에 적용할 기본값이 아니다.

## 4. 분류와 변환

원본 파일마다 아래 정보를 먼저 기록한다.

| 항목 | 내용 |
|---|---|
| 원본 | 받은 파일명 또는 실제 접근 가능한 원본 경로 |
| 카드 | 저장소의 정확한 이름 |
| 성별·용도 | m/f, 초상/casual/extra |
| 화풍 | 확정 키 또는 미확정 |
| 근거 | 생성 기록, 사용자 확정, 외형 추정 중 무엇인지 |
| 연결 | 일상컷이 어느 의인화 인물과 짝인지, 불확실한 부분 |
| 출력 | 최종 경로, 크기, 형식, 파일 해시 |

얼굴·헤어·체형·장비로 같은 인물의 컷을 대조하되, 애매하면 확정하지 않는다. 생성 기록이나 사용자 지시가 없는 화풍 키를 외형만으로 단정하지 않는다.

변환은 요청된 크기와 형식을 따른다. 비율을 유지하고 임의로 자르거나 늘리지 않는다. 구체적인 크기 조건이 없으면 원본 크기를 유지한다. 이미 조건에 맞는 WebP라면 불필요하게 재인코딩하지 않는다. 이름 변경만 필요한 경우 바이트 또는 기존 Git Blob을 그대로 재사용한다. 확장자만 바꾸지 말고 실제 WebP로 열리는지 확인한다.

## 5. 등록 순서

1. 대상 저장소·브랜치와 최신 커밋을 확인한다. 진행 중인 다른 변경이 있으면 보존한다.
2. 원본 수와 분류표를 대조하고, 확정된 이미지에만 최종 파일명을 배정한다.
3. 기존 `img/` 경로와 `data/img.json`의 같은 슬롯을 모두 확인한다. 같은 경로의 다른 내용은 자동으로 덮어쓰지 않는다.
4. 확정 파일을 `img/`에 배치하고 기체별 생성 기록을 준비한 뒤, 저장소 루트에서 아래 명령으로 원본과 기록을 검증한다. 이 단계에서는 자동 생성 파일을 최초 커밋 대상으로 만들지 않는다.

```bash
python3 make-thumbs.py --check
python3 register-images.py --check
python3 register-generation.py --check
node tests/per-mech-records.cjs
git diff --check
```

5. 대조 명령은 파일을 쓰지 않고 등록 가능 여부를 확인한다. 오류가 있으면 원인부터 수정하고 같은 대조를 다시 실행한다. 파일 누락 경고는 성공 코드와 함께 나올 수 있으므로 무시하지 않는다.
6. 출력과 최초 커밋 변경분을 한 번 확인한다. 대상 이미지 수·카드·화풍·성별·용도, 생성 기록의 원문 연결, 최초 커밋에서 파생 파일이 제외됐는지를 함께 본다. 정상 결과에 추가 반복 검사를 하지 않는다.
7. 원본 WebP와 필수 생성 기록만 **수동 커밋 하나**로 `refs/heads/main`에 직접 반영한다. 작업 브랜치와 PR은 만들지 않는다. 브라우저 다중 파일 업로드 또는 인증된 Git push를 우선하고, 둘을 사용할 수 없을 때만 Git Data API를 사용한다.
8. main 반영으로 시작된 자동화가 `img/thumb/`, `data/img.json`, `generation/index.json`을 생성·등록하고 `[skip actions]` 후속 커밋을 올리는지 확인한다. 이 봇 커밋은 정상 절차이며 수동 커밋 하나 원칙의 예외다. 성공 후 원격 파일·등록 항목을 재대조하거나 도감 화면을 열어보는 검수는 기본 절차가 아니다. 실패·경고·누락 등 구체적인 문제 또는 사용자 요청이 있을 때만 상세 조사한다. 자동화 대상이 아닌 문서 변경 등에 실행을 요구하지 않는다.

`--prune`은 기본 명령이 아니다. 파일 삭제·이동이 실제 작업 범위에 있고 누락 원인을 확인했을 때만 사용한다. 불완전한 체크아웃이나 다운로드 실패 때문에 기존 항목을 지우지 않는다.

### 원본과 생성 기록을 main에 직접 업로드한다

로컬 스크립트를 실행할 수 없다면 실행했다고 보고하지 않는다. 최신 파일명 규칙과 기존 슬롯을 직접 대조하고, 가능하면 코드 실행 환경에서 검증할 수 있게 작업을 준비한다.

#### 최초 수동 커밋은 입력 파일만 한 번에 반영한다

이미지 생성·교체 작업은 이미지 수와 관계없이 작업 브랜치와 PR을 만들지 않는다. 다음 입력 파일을 **수동 커밋 하나**에 담아 `refs/heads/main`에 직접 반영한다.

- `img/` 바로 아래의 최종 원본 WebP
- `generation/<기체ID>/settings.json`
- `generation/<기체ID>/images.json`
- `generation/<기체ID>/runs/<작업일시>/`의 실제 제출 프롬프트·설정 사본·보정 기록·매니페스트

`img/thumb/`, `data/img.json`, `generation/index.json`은 자동화가 만드는 파생 파일이므로 최초 수동 커밋에 포함하지 않는다. 로컬 검증 과정에서 이 파일들이 바뀌었더라도 업로드 대상에서 제외한다. 자동화의 `[skip actions]` 후속 커밋은 등록 결과를 보존하는 정상 커밋이며, 사용자가 요구한 “한 커밋”은 입력 파일을 올리는 최초 수동 커밋 하나를 뜻한다.

브라우저에서 GitHub의 다중 파일 업로드를 사용할 수 있으면 저장소 상대 경로를 유지해 위 입력 파일을 모두 한 번에 선택하고 `main`에 직접 커밋한다. 브라우저가 경로를 보존하지 못하거나 여러 커밋으로 나눠야 한다면 억지로 진행하지 않고 인증된 Git push를 사용한다. Git push도 사용할 수 없을 때만 아래 Git Data API 절차를 사용한다. 파일별 Contents API PUT처럼 입력 파일마다 커밋이 생기는 방식은 사용하지 않는다.

#### Git Data API는 대체 경로로만 사용한다

GitHub Git Data API는 반드시 다음 순서로 사용한다.

1. 최신 `refs/heads/main` 커밋과 그 tree를 조회한다.
2. 최초 수동 커밋에 포함할 원본 WebP와 필수 생성 기록만 각각 blob으로 만든다.
3. 최신 main tree를 `base_tree`로 사용해 변경 경로를 모두 담은 tree 하나를 만든다.
4. 최신 main 커밋 하나만 부모로 하는 commit 하나를 만든다.
5. 직전에 `refs/heads/main`이 같은 부모를 가리키는지 다시 확인하고, `force: false`로 ref를 새 commit으로 fast-forward 갱신한다.

확인 사이에 main이 바뀌면 강제 갱신하거나 오래된 tree로 덮어쓰지 않는다. 새 main의 tree에 입력 파일을 다시 합치고, 슬롯 충돌 가능성을 최신 내용 기준으로 재검증한 뒤 tree와 commit을 다시 만든다. Blob이나 tree, commit만 생성된 상태는 완료가 아니며 `refs/heads/main` 갱신과 자동화 성공까지 확인해야 한다.

- 같은 경로가 이미 있으면 자동으로 덮어쓰지 말고 3장의 슬롯 규칙에 따라 번호와 용도를 다시 확인한다.
- 다음 컷 번호는 raw가 아니라 API로 확인한다(캐시 때문): `GET /repos/polos0117/atelier/contents/data/img.json?ref=main`
- 원본과 생성 기록을 로컬에서 검증할 수 없으면 실행했다고 보고하거나 등록 결과를 손으로 흉내 내지 않는다. 원인을 기록하고 작업을 중단한다.
- 사용자가 별도의 Git 반영 방식을 명시한 경우에만 그 요청을 우선한다.

현재 자동 등록은 `main`에 WebP, `generation/m-*/settings.json` 또는 `generation/m-*/images.json` 변경이 반영되거나 수동 실행될 때 동작한다. 원본 이미지로 썸네일과 `data/img.json`을 만들고, 기체별 기록으로 `generation/index.json`을 만든 뒤 `[skip actions]` 커밋으로 되돌려 넣는다. 브라우저 업로드가 끝났거나 blob·tree·commit만 만든 상태에서는 완료가 아니며, main 반영과 해당 실행의 성공 여부를 확인한다. 실패·경고가 있으면 로그와 필요한 등록 항목을 조사한다. 화면은 자동화가 갱신한 `img.json`을 읽으며, 미등록 파일이 GitHub에 있다는 사실만으로 표시하지 않는다.

## 6. 기존 이미지 이동·재분류

기본 자리에서 `byStyle`로 이동하려면 **이동 대상 이미지의 화풍 근거**가 있어야 한다. 기존 대장의 카드별 값만으로 일괄 이동하지 않는다.

목표 경로와 슬롯 충돌을 먼저 확인한다. 기존 `face` 좌표와 성별·컷 연결을 보존하고, 불필요하게 이미지를 재변환하지 않는다. 이동 전후 파일 해시 또는 Blob SHA가 같음을 확인한다. 이것은 내용 보존 검증이며 화풍 분류가 정확하다는 증명은 아니다.

기존 이름 제거와 새 이름 등록이 일관되게 반영됐는지 확인한다. 삭제·교체가 요청 범위를 벗어나면 임의로 진행하지 않는다.

## 7. 중단 대비와 재개

신규 생성 설정·원문 연결은 [generation/README.md](generation/README.md)의 기체별 구조를 사용한다. `settings.json`은 최신 설정, `runs/<작업일시>/`는 변경하지 않는 당시 기록, `images.json`은 해당 기체의 이미지별 원문 연결이다. `data/img.json`의 이미지 등록 절차는 그대로 따른다.

여러 장 작업에서는 생성이 끝날 때까지 기다리지 말고 묶음마다 분류표와 체크포인트를 저장한다. 파일명은 작업별로 정하되 실제 저장 위치를 남긴다.

체크포인트는 기존 생성 매니페스트에 원본/변환본 위치, 최종 경로, 파일 해시, 대상 ref와 진행 상태를 모아 기록한다. 확인된 커밋 SHA는 완료 시 남긴다. 일반 Git 작업에서는 Blob·Tree SHA를 별도로 조회하거나 단계별 점검 문서를 만들지 않는다. GitHub API로 Blob·Tree를 직접 생성하는 경우에만 반환된 값을 복구용으로 보존한다.

원본과 변환본이 다른 대화에서 접근 가능한 곳에 실제로 보존됐는지 확인한다. 임시 작업 경로만 적어 두고 복구 가능하다고 보고하지 않는다. 재개할 때는 체크포인트와 원격 상태를 대조하고, 이미 존재하는 파일/Blob을 재사용한다.

## 8. 완료 보고 기준

**Blob·Tree·Commit 생성만으로 업로드 완료라고 말하지 않는다.** `refs/heads/main`이 새 commit을 가리키는지와 등록 자동화 성공 여부를 확인해야 한다.

생성 수/요청 수, main 반영 여부와 커밋, 문제·대기 항목만 짧게 보고한다. 정상 완료 시 검사 명령이나 세부 진행 상태를 나열하지 않는다.

commit까지 만들었지만 main ref를 갱신하지 못했으면 “commit 생성 완료·main 미반영”, 등록을 기다리면 “main 반영 완료·등록 대기”라고 구분한다. 직접 확인하지 않은 배포나 화면 반영까지 완료로 확대하지 않는다.

`prompt.html`에서 직접 생성하는 작업은 [PROMPT_IMAGE_WORKFLOW.md](PROMPT_IMAGE_WORKFLOW.md)의 생성·화풍 비교·참조 연결 절차도 먼저 읽는다.
